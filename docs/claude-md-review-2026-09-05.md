# `CLAUDE.md` — contract-gate record, 2026-09-05

The loop log for this project's `CLAUDE.md` lives here rather than in the
document, for the reason `docs/claude-md-review-2026-09-04.md` gives: the
file is read in full by every session on every turn.

**This is a new run, and the loop numbers continue that record's** — they
are the document's history, not this run's. The 2026-09-04 run reached a
violent cap, which ends the review of the text it was measured against;
it does not attach to the document forever. An authoring edit that changes
direction re-arms the gate normally, and this is one.

**Trigger.** `In flight:` stopped being kept by hand and now reads off the
roadmap, and a § Running two sessions at once was added. A conformer now
records position differently and must check the roadmap before picking
work up, so rule 14's gate was owed.

## Loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 4 | 2026-09-05 | 3, cold — genre pinned `standard`; packet carried the measured `git config` output for both worktrees, the `pre-push` source, `workflow.md` § 1 and `scripts/ci.sh`'s head | 3 | 2 | 2 | n/a | **Seven verified, seven fixed; none dismissed.** **All three lanes independently found the same defect, and it is the one the author missed entirely:** § Running two sessions at once permits two items in flight, and `workflow.md` § 1 says *"Exactly one item is in flight at a time. A second waits."* — a standard this very file cites as its authority two sections earlier, and whose departures § Overrides requires to be recorded in `docs/standards/`. Nothing was recorded. A second session reading the standard waits; reading this file it starts. The override is now stated in the section and filed as `docs/standards/workflow-overrides.md`, which answers the standard's actual objection rather than asserting an exemption: § 1's worry is that project state becomes unanswerable, and that is an objection to state being a scalar somebody remembers — once `In flight:` is a query, a set of two is as answerable as a set of one. **All three also found `State:` left as a stored literal** (`4 — between items`) three lines above prose calling it derived, which is the same two-places-at-once failure the rewrite claimed to have removed, now doubled across two sessions. It is written as the formula. **All three found a pre-existing Q1 that this session's own measurement had already falsified without the author noticing:** the document says an unset `core.hooksPath` means no hook runs, and on this machine `~/.gitconfig` sets it machine-wide, so unsetting the repository value falls back to the machine-wide gate rather than silencing it — and the same fact breaks *"all in `.git/config`, none surviving a clone"* for that one key. A session diagnosing a silent push was being sent after a cause that cannot produce silence. **Two lanes found the new section double-counting the settings** — *"the four `ants.gate.*` settings"*, where four is the total including `core.hooksPath` and there are three — in the same paragraph that warns a missing gate is silent. **Two found `${ANTS_GLOBAL_HOOKS:-...}` substitutes on empty as well as unset**, so *"set to anything at all"* is wrong, and one added that the resolved path must also be executable. **Two Q3s were gaps the new section opened and nobody could have breached knowingly:** nothing said when 🚧 is cleared or by whom, so a stale marker silently removes an item from the other session's queue; and nothing said what `Next:` does when a session picks up an item that line does not name. Both are now rules 5 and 6. **Open questions settled, none a finding:** `Lanes:` is a real field on this project's items, so rule 4 has a signal; and `--docs` firing no compiler leg was confirmed by observation earlier the same day, a documentation-only push running the quarantine guard and the link check and nothing else. |
| 5 | 2026-09-05 | 3, cold — identical brief, packet rebuilt from disk and extended with the new `docs/standards/workflow-overrides.md` | 0 | 2 | 2 | n/a | **Four verified, four fixed; none dismissed. Every one landed on text loop 4 wrote**, which is the collateral pattern this loop exists to catch, and none was pre-existing. **All three lanes found the same two.** Loop 4 added rules 5 and 6 to a list introduced as *"Four rules"* and did not change the count — so a session reading the count as authoritative treats the two new rules as commentary, and both are ones whose breach is invisible: a stale 🚧 silently removes an item from the other session's queue, and `Next:` gets overwritten. And loop 4's own override document set a cap — *at most one per session* — that appears nowhere in the always-loaded rules, so a session could take a second item, believe it complied, and breach `workflow.md` § 1 with no cover. The cap is now in rule 2. **Two lanes found the merge route unperformable as written:** rule 3 put every session in a worktree while the paragraph below assumed one session holds `main`, and merging *into* `main` from a worktree cannot be done — it is checked out elsewhere, and a push at it is refused, confirmed by dry run. The additional session now pushes its own branch and the session holding `main` merges it. **One lane found rule 5's two dispositions not exhaustive:** an item still under way at a session boundary had no compliant state, and 📋 would have advertised a half-built item as free — `workflow.md` § 1's state 5 persists, and rule 5 now says so. **A lane's open question found the gap in this run's own verification, and it was the right one to ask:** loop 4 proved the gate SETTINGS reach the second worktree and never proved a push from it was gated, which rule 3 itself calls the thing most likely to be assumed because a missing gate is silent. Measured: a push from that worktree fires the hook, runs `ci.sh --docs` and names the commit it gates. **Settled for all three lanes, not a finding:** `--docs` fires no compiler leg — `scripts/ci.sh` exits at its docs branch well before the first `cmake`. |
| 6 | 2026-09-05 | 3, cold — identical brief, packet rebuilt from disk | 2 | 4 | 5 | n/a | **Eleven verified, eleven fixed. Cap reached (3 for a standard); the run ships.** **The most important finding was the orchestrator's, not a lane's, and no lane could have found it — it is a runtime fact rather than a document claim.** Lane 3's Q3 about `ROADMAP.md` being a guaranteed shared write prompted a test of what a roadmap verb actually does from the worktree, and the answer invalidated the design this gate was reviewing: the store is keyed to the MAIN checkout, a worktree has no store row, and `roadmap_log` there silently falls back to patching that worktree's own `ROADMAP.md`, which the next render overwrites. A query from the worktree answers `source: "markdown"` where the main checkout answers `source: "store"`; a dry-run flip reports `write_path: "patch"`. So the second session's claim would have vanished with nothing said, and the roadmap — the whole coordination mechanism — would have coordinated nothing. Every roadmap verb now passes the main checkout as `caller_cwd`, and only the main checkout commits `ROADMAP.md`. **Two lanes found the cap stated per session and never as a total**, so a third session passes every check, takes a third item and breaches the override with nothing able to catch it. **Two lanes found rule 4 lacking the precedence clause rule 2 has**, so two sessions facing a lane collision at the top of the priority order settle it differently. **One lane found rule 6's premise false:** `Next:` is not one line both sessions see — each worktree has its own checkout on its own branch, confirmed here by two inodes — so the rule prevented nothing and the mechanism is now that only the main checkout advances it. **One lane found 🚧 records THAT an item is held and not by whom**, which rule 5 makes worse by design since the marker outlives a session: an abandoned half-built item and a live claim are byte-identical. The flip now names its holder, and a holder absent from `ListAgents` marks the item resumable. **One lane found rule 3 stated no check at all**, so a session started the ordinary way in the main checkout has breached before reading the rule. **Two lanes found § Overrides scoped to "a global standard", which by this file's own taxonomy excludes `workflow.md`** — so the override this very run filed was itself unsanctioned. **And the empty-directory test, dismissed three times in the 2026-09-04 run as immaterial, was fixed rather than dismissed a fourth time**: the claim is false, the fix is one clause, and four reports cost more than the edit. **A VIOLENT cap — every one of the eleven landed on text this run wrote.** The section is one day old, so that is expected rather than alarming, but it is why the section was rewritten whole at this loop instead of patched an eleventh time. No further gate on this text: it goes to use, and the second session running under it is the next reviewer. |

## The same harness limitation as the 2026-09-04 run

That record's closing section applies unchanged: the harness injects the
project `CLAUDE.md` into every subagent's system context, so the lanes
were **not fully cold** on this document. What they held was the version
at session start — the pre-change text — so every finding above was still
made against text they had to read fresh, and the strongest finding of
the run was a contradiction with a *different* document.
