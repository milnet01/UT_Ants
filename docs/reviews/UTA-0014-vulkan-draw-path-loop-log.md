# UTA-0014 — Vulkan draw path — loop log

The review record for `docs/specs/UTA-0014-vulkan-draw-path.md`, kept outside
the spec per `~/.claude/standards/spec-format.md` § 6. `review-contract` writes
one row per loop as it closes. Rows are never back-filled and a landed row is
never edited.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-12 | 3 | 1 | 6 | 2 | 3 | 12 verified, 1 dismissed, 12 fixed. **All three lanes independently found the same three.** The MSVC leg could not configure at all: `find_package(Vulkan 1.3 REQUIRED)` is a configure-time requirement on every leg that builds `uta_urender`, while SS 4.12 gave Windows nothing new -- the leg would have gone red before compiling anything, and ADR-0007 route 3 already names the LunarG SDK for it. `Camera` was used in the public `draw` signature and defined nowhere, which is exactly what UTA-0016 and uworld must bind to. And `draw`'s comment promised a returned colour target its `Result<void>` signature cannot give, inviting a VkImage into a public header against INV-2. **Three more were vacuous-test shapes.** INV-1's C++ test could not observe a CMake link set, and `uta_unit_tests` already links uta_upkg, uta_umat and uta_ubake, so it would have passed under a breaching build; the configure assertion is now its only grader. INV-5's rule made its own grading test unable to pass, RenderDeviceAbsentTest being run with no device on purpose -- it now carries the label `device-absent`. INV-6's tolerance was calibrated FROM the implementation under test, so a real sign or clamp error would have been rounded up into the bar and shipped green; it is now a fixed 1e-3 ceiling derived from SS 4.3's [0,1] unit. **The rest.** Device-test registration guarded on finding a loader would have reintroduced the green-over-nothing hole SS 3 decision 6 exists to forbid, so registration is unconditional and a missing loader fails the configure. `Config::surface` was a VkSurfaceKHR in a public header, now an integer handle. Pipeline identity was keyed on material while polyFlags is what selects cull, blend and depth state. INV-2's test shared a binary with INV-3's Vulkan fixtures, so it could never fail. The offscreen format was unnamed while INV-10 compares a literal pixel. And SS 4.1's assertion, copied literally from ubundle's STREQUAL form, would have failed configure on a CONFORMING build here. **Dismissed** as true but immaterial: "the one test in this project that links both a build-time and a runtime library" is false, uta_unit_tests already doing so -- the clause was deleted in passing rather than fixed. **Two collateral repairs the orchestrator's own sweep and section re-read caught, not a lane:** SS 3 and SS 4.12 pointed at a SS 9 gap row that did not exist, and SS 4.12 still said the MSVC leg registers no device tests after SS 7 changed to register-and-deselect. **Unrunnable region declared up front and honoured:** no `src/urender/` exists, so no claim about `uta_urender`'s behaviour is checkable by a lane, and the Windows leg cannot be exercised from this machine. Lane yields 6 / 5 / 6; each reported reading all 902 lines. Phase 1b yield zero -- every citation windowed and resolved. Four open questions resolved clean: BlockFormat is exactly BC4/BC5/BC7; the PolyFlags values were verified against Surreal independently and agree with UTA-0109 on all five it names; the DOOM_Ants citations were verified verbatim by the orchestrator; glm's FetchContent placement is resolved by the prose four lines later. |
