# UTA-0014 — Vulkan draw path — loop log

The review record for `docs/specs/UTA-0014-vulkan-draw-path.md`, kept outside
the spec per `~/.claude/standards/spec-format.md` § 6. `review-contract` writes
one row per loop as it closes. Rows are never back-filled and a landed row is
never edited.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-12 | 3 | 1 | 6 | 2 | 3 | 12 verified, 1 dismissed, 12 fixed. **All three lanes independently found the same three.** The MSVC leg could not configure at all: `find_package(Vulkan 1.3 REQUIRED)` is a configure-time requirement on every leg that builds `uta_urender`, while SS 4.12 gave Windows nothing new -- the leg would have gone red before compiling anything, and ADR-0007 route 3 already names the LunarG SDK for it. `Camera` was used in the public `draw` signature and defined nowhere, which is exactly what UTA-0016 and uworld must bind to. And `draw`'s comment promised a returned colour target its `Result<void>` signature cannot give, inviting a VkImage into a public header against INV-2. **Three more were vacuous-test shapes.** INV-1's C++ test could not observe a CMake link set, and `uta_unit_tests` already links uta_upkg, uta_umat and uta_ubake, so it would have passed under a breaching build; the configure assertion is now its only grader. INV-5's rule made its own grading test unable to pass, RenderDeviceAbsentTest being run with no device on purpose -- it now carries the label `device-absent`. INV-6's tolerance was calibrated FROM the implementation under test, so a real sign or clamp error would have been rounded up into the bar and shipped green; it is now a fixed 1e-3 ceiling derived from SS 4.3's [0,1] unit. **The rest.** Device-test registration guarded on finding a loader would have reintroduced the green-over-nothing hole SS 3 decision 6 exists to forbid, so registration is unconditional and a missing loader fails the configure. `Config::surface` was a VkSurfaceKHR in a public header, now an integer handle. Pipeline identity was keyed on material while polyFlags is what selects cull, blend and depth state. INV-2's test shared a binary with INV-3's Vulkan fixtures, so it could never fail. The offscreen format was unnamed while INV-10 compares a literal pixel. And SS 4.1's assertion, copied literally from ubundle's STREQUAL form, would have failed configure on a CONFORMING build here. **Dismissed** as true but immaterial: "the one test in this project that links both a build-time and a runtime library" is false, uta_unit_tests already doing so -- the clause was deleted in passing rather than fixed. **Two collateral repairs the orchestrator's own sweep and section re-read caught, not a lane:** SS 3 and SS 4.12 pointed at a SS 9 gap row that did not exist, and SS 4.12 still said the MSVC leg registers no device tests after SS 7 changed to register-and-deselect. **Unrunnable region declared up front and honoured:** no `src/urender/` exists, so no claim about `uta_urender`'s behaviour is checkable by a lane, and the Windows leg cannot be exercised from this machine. Lane yields 6 / 5 / 6; each reported reading all 902 lines. Phase 1b yield zero -- every citation windowed and resolved. Four open questions resolved clean: BlockFormat is exactly BC4/BC5/BC7; the PolyFlags values were verified against Surreal independently and agree with UTA-0109 on all five it names; the DOOM_Ants citations were verified verbatim by the orchestrator; glm's FetchContent placement is resolved by the prose four lines later. |
| 2 | 2026-09-12 | 3 | 0 | 6 | 3 | 1 | 10 verified, 0 dismissed, 10 fixed. **At the cap for a spec, and a CALM one: 1 of this loop's 10 verified findings landed on text this run wrote** (readback's contradiction, from loop 1's own fix 11, which added "whatever the swapchain's own format is" while readback says "Surfaceless path only"). The other nine anchor on text present in the original draft at eaa6363. **Second share: 22 of the run's 22 verified findings lie inside the gated span**, which is the whole document, the file being new -- so this run was gate and audit at once and the two are not separable. **All three lanes found INV-10 unpassable:** exposure and the Khronos PBR Neutral tone map sit between the two ends of the transfer and neither is the identity, so a texel drawn unlit does not come back as stored. Resolved by stating that the output stage applies to unlit surfaces and sky too (a per-batch bypass would change what a real frame looks like) and adding Config::linearOutput, which INV-10 now names as load-bearing. **Two lanes** found the glm acquisition row naming tests/CMakeLists.txt while the prose moves it to the root -- raised as an open question in loop 1 and wrongly resolved clean by the orchestrator, which is the dismissal the cold re-read exists to check. **Lane E alone** found INV-7 device-free while SS 4.7's ambient-cube evaluation is per-pixel shader arithmetic, so it could only have graded the C++ copy SS 3 decision 5 forbids and INV-6 was made a device test to avoid; INV-7 is now a device test on an included probes.glsl. Lane E also found that nothing deselects the device tier: scripts/ci.sh is the whole gate, its ctest call carries no -L, and SS 11 never named the file -- so the MSVC leg would have run device tests with no driver, which INV-5 requires to fail. **Lane F alone** found SS 1 and SS 9 giving the swapchain to UTA-0016 while SS 4.1 and SS 4.3 keep it (scope narrowed to the window and its surface), and INV-11 grading the velocity target when nothing could observe it (readback now takes a Target). Three constants the document called "stated" were stated nowhere: the masked alpha threshold is now 0.5, justified from src/umat/Resolve.cpp writing binary alpha so only filtering produces intermediates, and the cluster grid is 16x8x24 with a per-cluster cap of 64. Nothing says how SPIR-V reaches the binary (now embedded as a generated header, so no run-time search path exists for UTA-0016's packaging to bind to). And SS 4.4 promised a refusal naming the device and the failed requirement, which INV-5's own measured case cannot satisfy -- vkCreateInstance failing has no device to name; three refusals are now distinguished. Final 4b sweep before exit: yield zero. Mechanical checks clean (0 findings, 11 invariants, SS 5 / SS 7 / SS 10 id sets identical by diff). Size at the cap: 1093 lines against siblings at 470 to 997 -- longer than every sibling and past the 1000-line mark SS 5.4's evidence attaches to, reported here because the measurement is owed at the cap. Not split: for a spec the cap routes to implementation rather than to a split, and a split would need roadmap ids write-spec may not allocate. Status: accepted at the cap. |
| 3 | 2026-09-12 | 3 | 2 | 2 | 2 | 1 | Re-gate of the amendment recording what implementation found (baseline 76423fe); loop 1 of that run. 7 verified, 1 dismissed, 7 fixed. **All three lanes found three.** SS 4.10 put the unit surface at 0.978 / sRGB 253 through PBR Neutral at exposure 1.0; a mirror of post.frag's pbrNeutral gives 0.869 / 240, and 255 first at exposure 13.5 -- the sentence is deleted and the draft rationale corrected. SS 4.8 re-planned the shadow atlas only on a tile-size change, while Shadows.cpp also re-plans on a changed light set or any field sameLight compares. Resize had no call carrying a new size, and a surface may report currentExtent 0xFFFFFFFF; SS 4.3 now has Renderer::resize, taking effect at the next draw. **All three also found** INV-5 and SS 6 promising a named requirement that SS 4.4's own first refusal cannot give. **Lane B alone** found INV-5's test grading create's refusal and never the rule that a `device` test fails rather than skips; tests/CMakeLists.txt now registers a device-absent grader running INV-4's test with no driver, passing only on requireRenderer's compact `failed:` line -- a SKIP mutant of requireRenderer goes red, where a first regex on the message alone let it survive. **Lane C alone** found Config's two new fields each claiming to select the surfaceless path; createSurface now selects, and a mismatched Config is refused InvalidArgument. **Lane A alone** found SS 7's "`device` added where a driver is installed" readable as driver detection, which INV-5 exists to forbid, beside present-tense "no `-L` today" claims in SS 7 and SS 11 that the build made false. Dismissed: the DOOM_Ants Makefile path (immaterial). Open questions not filed: stale INV-2 binary list and SS 3's glm search claim (nothing built differently); two stale code comments (Frame.cpp exposure, Pipelines.cpp normal offset) corrected outside the spec. 4b sweep: SS 7 and SS 10 INV-5 rows updated. Mechanical checks clean. Loop 2 follows, cold. |
