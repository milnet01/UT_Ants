# `dependency-acquisition.md` — loop log

The review record for `docs/standards/dependency-acquisition.md`, kept
outside the document per `~/.claude/standards/documentation.md` § 9.1.
`review-contract` writes one row per loop as it closes. Rows are never
back-filled, and a landed row is never edited.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-13 | 3, cold — genre pinned `standard`; packet: `docs/design.md` § The stack, ADR-0008, `dependencies.md` §§ 1, 4, 8 and 9, `docs/standards/README.md`, the root, `src/urender`, `src/umat` and `tests` CMake files, `ci.yml`, `scripts/ci.sh`'s docs exit and configure step, README § Building it, `third_party/bc7enc/README.md`, and `FindVulkan.cmake` excerpts. No unrunnable region for this tree | 0 | 1 | 6 | n/a | **Seven verified, seven fixed, none dismissed.** All seven anchor in the gated span, commit 71500fb, which drafted the document. **Five of the seven were on § 2's routing question.** Question 1 keyed route 3 on version matching, which the loader it exists for does not clearly meet; it now keys on being the platform's driver stack. Question 2 read *a* route-3 acquisition where the SDK and the distribution packages supply different things; it now reads *every* acquisition § 5 accepts. Question 4 left a header-only `INTERFACE` target undecided; glm's own CMake makes `glm::glm` one when its library build is off, and it now counts as linked. Question 5 contradicted its own test-only sentence and read every library target as non-runtime; route 4 is now one optional executable behind a build option of its own. **Two elsewhere:** the opening now puts toolchain libraries such as `Threads` out of scope, and § 5 names the Windows runtime DLL that `ci.yml` installs and checks in its own step. One more rule came from the ADR-0008 run and landed here, where it lives: § 5 now requires `README.md` to name the three inputs and both acquisitions. **Clean-resolved open questions:** bc7enc's licence file is present, `scripts/ci.sh` has no Vulkan probe, and the uncovered What-checks-this rows are self-assessable. **Collateral:** ADR-0008's route 4 wording, tightened to match question 5. **Out of scope, corrected in passing:** `docs/design.md`'s index prose listed the validation layers among found inputs. |
| 2 | 2026-09-13 | 3, cold — briefed identically; packet rebuilt whole from disk with the subject re-measured, adding `docs/design.md`'s runtime-target definition, glm's `INTERFACE` block and the root file's option list | 1 | 0 | 3 | n/a | **Four verified, four fixed, none dismissed.** **Three of the four landed on text loop 1 wrote.** All three lanes found loop 1's question 5 contradicting itself for an editor under `tools/`, which is behind both `UTA_BUILD_TOOLS` and an option of its own; the exclusion now names a tool whose only guard is `UTA_BUILD_TOOLS`. Two lanes found loop 1's question 2 keyed on an open set — *every acquisition § 5 accepts* names no distribution, so SDL3's route turned on which one the reader pictured — and one found its pin clause circular; question 2 now asks whether it is a tool of the platform's Vulkan toolchain, and a library never is. **[Q1] The fourth was in the draft:** § 5 said one `find_package` asserts all three inputs at the floor, while `FindVulkan` reads `Vulkan_VERSION` from `vulkan_core.h` alone; § 5 and What checks this now say the loader's and `glslc`'s versions are unchecked. **Clean-resolved open questions:** `src/urender` is added unconditionally; `docs/design.md`'s Vulkan row reads *Installed SDK* as shorthand the prose beneath it expands. **Collateral, from the 4b sweep:** `docs/design.md`'s index prose still stated route 1 by the old version-matching test. |
| 3 | 2026-09-13 | 3, cold — briefed identically; packet rebuilt whole from disk with the subject re-measured, adding `FindVulkan`'s version-detection block | 0 | 3 | 1 | n/a | **Four verified, four fixed, one dismissed. Cap reached (3 for a standard); the tail is empty and the run exits.** **[Q2] All three lanes found question 2 admitting `glslc` because it needs *no particular version* while § 5 held it to the Vulkan floor**; § 5 now puts the headers and loader at the floor and `glslc` present, question 2 reads *no pinned version*, and What checks this names only the loader's version as unchecked. **[Q2] Two lanes found § 1's *a build input is asserted where the build configures*** against § 3, which asks only that SDL3's system headers be named in the README; the principle is now scoped to route-3 inputs. **[Q2] One lane found question 5 sending Assimp to route 1** if `ut-ed` links it through its `ued` part, which `docs/design.md` lists as a part and parts build as library targets; route 4 now asks whether everything that links the dependency is built only behind one option of its own. **[Q3] One lane found *toolchain* used in two senses**; the scope sentence now names the C++ compiler's own libraries. **Dismissed:** What checks this says removing `GLOBAL` still configures, which is false while `tests/` links `Vulkan::Vulkan` — true, and it changes no line a conformer writes. **Cap measurements.** **Three of the final loop's four findings landed on text this run wrote** — question 2 from loop 2, question 5 from loops 1 and 2, the scope sentence from loop 1 — so this is a **violent** cap: § 2's routing question was repaired on every loop, as it was on ADR-0007's before it. **Against the gated span, commit 71500fb, nine of the run's fifteen verified findings anchor on text as drafted there**; the other six anchor on this run's own edits. **So this review ends here, and the gate is not re-run on the document as it stands.** Size is not the signal: the document is short. The unsettled part is § 2, and the next dependency to land, SDL3, routed through it by hand, is the better reviewer than a fourth cold read. **Collateral:** ADR-0008's route 4 and Supersedes line, reworded to match question 5, and its route 3, which still held `glslc` to the Vulkan level; the at-cap sweep over the run's topic words found only that one. |
