# UTA-0008 — `ubundle`: the `.utab` container, its origin field and its version — loop log

The review record for `docs/specs/UTA-0008-bundle-container-and-origin.md`,
kept outside the spec per `~/.claude/standards/spec-format.md` § 6.
`review-contract` writes one row per loop as it closes. Rows are never
back-filled and a landed row is never edited.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-08 | 3, cold — genre pinned `spec`; packet carried design.md's § The bundle, rules 1/2/3, 15, 16–19 and the § Close calls naming bullet, versioning-overrides.md's milestone table and § Breaking surfaces, UTA-0007 §§ 4.2/4.6/5/6/7/9, UTA-0006 §§ 4.5/4.6/4.7/14, the whole of `Rooms.h` and `Graphs.h`, `Error.h`, and both CMakeLists; the absent reader and writer were declared an unrunnable region up front | 1 | 3 | 1 | 5 | **Ten verified, ten fixed, one dismissed. First gate on this document.** **All three lanes independently found the same two defects, and both were vacuous fixtures rather than wrong prose.** The first: INV-1's *Test* was a corpus of truncations claimed to isolate § 4.2's per-read bounds check — but § 4.4's table rules reject every truncation before a payload byte is read, so deleting the § 4.2 bound leaves the whole corpus red and the mutation reads as killed. INV-1 now carries two corpora with the rule each grades named, the isolating one having a table consistent with the file size and a payload that runs out mid-element. This is the failure `CLAUDE.md` § Build and test records from UTA-0007, reproduced in a document written by a session that had just read it. The second: **INV-6's golden fixture covered three types while its own *Breaks when* named swaps in two others** — `minZ`/`maxZ` are `Room`'s and `x`/`y` are `Point2`'s, so a reader swapping either passed INV-6, passed INV-7 on the same bytes, round-tripped under INV-8, and shipped — the exact UTA-0078 class § 4.6 cites. The fixture now carries one instance of every type §§ 4.6–4.8 encode, and the *Breaks when* lists every same-width adjacent pair by name instead of counting them; one lane found the count stated three incompatible ways (§ 4.6 "four pairs", § 4.7 "the second pair", INV-6's six-item list). **All three also found INV-3 overclaiming against § 4.9** — it promised every stored index bounded while § 4.9 left `Room::zoneIndex` unbounded and non-unique and never mentioned `Node::iZone`. Verifying it settled the shape: `src/umap/Rooms.cpp`'s `roomForZone` already guards zone 0, `ZONE_REFUSED` and `>= roomForZone.size()`, so `iZone` is total at the accessor and a load-time refusal would reject bundles the runtime handles; `zoneIndex` has no such guard and is now bounded, required distinct, and required to agree with `roomForZone`. INV-3 is scoped to § 4.9's list, and § 4.9 states the one exception with its reason and contrasts it with `edgesFrom`, which has no guard of its own. **Two lanes found INV-8's cross-compiler half unfalsifiable**: encoding one bundle twice inside a process compares each compiler with itself and can never see GCC's bytes differ from MSVC's, while INV-8's named defect (a `memcpy`d struct's padding) is per-compiler and passes on all three legs. Cross-compiler byte-identity moved to INV-7, whose golden array is a literal fixed in source, and § 10's two rows moved with it. **One lane found the § 4.4 count check written as `16 + 24 * sectionCount <= fileSize`** — the multiplication form § 4.2 rule 1 forbids by name and INV-2's *Breaks when* names as its own defect, in a `u32` where `24 * 0xAAAAAAAB` wraps to 8 and admits 2.8 billion descriptors; it is now a division. **One found the string encoding unspecified** [Q3]: nothing said whether the reader validates UTF-8, and the strings come from a UE1 name table that guarantees no encoding, so one implementation would refuse what another wrote. They are now opaque and round-tripped verbatim. **Three came from resolving lane open questions, and each was a real defect:** INV-9 required a quiet NaN to round-trip through *every* `f32` while § 4.9 requires `bands` ascending, so its own fixture was refusable — the NaN cases are now scoped to fields § 4.9 does not order; § 6 credited the section-extent refusal to INV-11, which named only unknown id, duplicate id and overlap; and **nothing anywhere graded `combine`, the `kind` range check, the `reserved` zero checks or the `compression` byte**, all four stated in § 6 with no invariant. INV-12 now locks them, and writing it found a live trap: with `Derived = 0` and `Authored = 1`, a `combine` written as `max` or bitwise OR returns `Authored` for (`Authored`, `Derived`) — the publishable value, from an input that touched an install, which is the precise inversion § 3 decision 4 chose the encoding to prevent. **One dismissed, recorded:** § 4.7 attributes the `pruned` reasoning to UTA-0006 § 4.5 where it sits in § 4.1 — true, and the encoding built is a `u8` either way; two lanes found it and both declined to file it on the same ground. **Four open questions resolved clean and are not in the tally**: `Room::floors` non-empty matches UTA-0007's INV-8 verbatim, `bands` cannot be empty while rooms exist (§ 4.5 step 5 opens a band at z = 0), `iZone` is guarded at the accessor, and `tests/CMakeLists.txt` line 46 confirms the `unit;fast` label. **One packet defect, mine:** the brief declared the subject 791 lines against an actual 792, and all three lanes reported the discrepancy; none was misled, each having anchored on section headings. Document grew 792 to 878 lines — every added passage closes a verified finding, and the largest two are INV-1's two-corpora split and INV-12. |
| 2 | 2026-09-08 | 3, cold — same brief, packet rebuilt from disk and re-measured at 878 lines; no lane was told what loop 1 fixed. Four windows added that loop 1's lanes had asked for as open questions — `src/umap/Rooms.cpp`, UTA-0007 § 4.5, UTA-0006 § 4.1 and `tests/CMakeLists.txt` — all source, no review history | 3 | 1 | 1 | 4 | **Nine verified, nine fixed, two dismissed. THE CAP (2 for a spec), and it is a CALM one with one oscillating passage.** **Share of this loop's findings landing on text THIS RUN wrote: three clearly (INV-6's fixture, INV-8's *Test*, INV-6's completeness claim), one mixed (the § 4.4 ordering rule is original, the INV-11 sentence it anchors on was rewritten in loop 1), five on original draft text — so 3–4 of 9, anchors checked against loop 1's ledger rows rather than recalled.** The document held more defects than the cap held loops, which is what a calm cap means; the oscillation is real but localised to the two invariants loop 1 rewrote most heavily. **Second share, against 1c's recorded span: 9 of 9.** The span is the whole file — the document is new — so this run was gate and audit at once by construction, and that number carries no verdict. **All three lanes found the same defect, and loop 1 created it.** Loop 1 widened INV-6's golden fixture to "one instance of every type §§ 4.6–4.8 encode" — which § 4.9 makes unsatisfiable: with one `NavNode`, `nodes.size()` is 1, so every edge's `from` and `to` are pinned to 0 and the transposition INV-6 exists to catch cannot be expressed; with one `Node` and one `leafZone` entry, `iFront`, `iBack` and both `iLeaf` slots draw from `{INDEX_NONE, 0}`. An implementer would have found distinctness impossible and quietly relaxed it, producing exactly the fixture INV-6 calls useless. INV-6 now states a multiplicity floor and scopes distinctness to the fields § 4.9 leaves free. **One lane found INV-6's completeness claim false** — loop 1 wrote "This is the complete list for this format" and it omitted `Node`'s `normal.z` with `w`, `NavEdge`'s `distance`/`collisionRadius` and `collisionHeight`/`reachFlags`, and `WiringNode`'s `firstOutgoing`/`outgoingCount`; § 7 tells an implementer to build the mutation set from that list. The list is replaced by the RULE that generates it — every run of two or more consecutive fields sharing a width and a type — so it cannot go stale when a layout gains a field. **One found INV-8 still unable to catch its own named defect**: encoding one object twice reads the same padding both times, so a `memcpy`ing writer passes; it now encodes two independently constructed equal `Bundle`s. **One found INV-2's fixture could not grade the multiply-versus-divide half** — `0xFFFFFFFF * 20` wraps to 4294967276, still far above the bytes remaining, so a multiplying reader rejects it too; a second case uses `0xCCCCCCCD`, whose product wraps to 4 and which only a dividing check refuses. **One found the ascending-offset rule graded by nothing** while § 4.4's overlap check is a linear pass that assumes it; it is now an INV-11 case with a § 6 row. **One found § 13's memory claim false** — "roughly twice the file size" ignores that every string- and vector-bearing element pays fixed in-memory overhead the file does not, and UTA-0016 sizes `ut-ants`'s working set from that line. **Three came from lane open questions and each was real:** § 3 and § 8 claimed design.md rule 17 "assigns *both* kinds of change" to a version bump, where rule 17 attaches that phrase to the model-type clause alone and says only that framing is `ubundle`'s — a warrant, not the proof it was written as; § 4.4 said nothing about gaps or trailing bytes, so two conforming readers would differ, and the sections now tile the file exactly; and `readHeader`'s validation scope was unpinned, where applying § 4.4's `sectionCount` bound inside it would refuse every valid bundle and, under § 4.5's fail-closed rule, make the quarantine guard block every push of an `authored` bundle while appearing to work. **Two dismissed:** § 4.2's minimum-size table omits `vector<Point2>`, derivable from rule 1's own wording; and § 4.7's attribution of the `pruned` reasoning to UTA-0006 § 4.5 rather than § 4.1 — immaterial by the Purpose test, and four lanes across both loops declined to file it on that ground. **The citation was nevertheless corrected at the exit**, as a documentation fix outside the gate's fix pass rather than as a finding, because the gate ends here and nothing re-reads the document after it; recorded so the correction is visible rather than silent. **One packet defect, mine, in loop 1:** the brief declared 791 lines against 792. Loop 2's packet was re-measured. 878 to 954 lines. |
