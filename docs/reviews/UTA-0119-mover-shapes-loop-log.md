# UTA-0119 — `ubake`: bake each mover's own shape — loop log

The review record for `docs/specs/UTA-0119-mover-shapes.md`, kept outside the
spec per `~/.claude/standards/spec-format.md` § 6. `review-contract` writes one
row per loop as it closes. Rows are never back-filled and a landed row is never
edited.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-11 | 3 | 1 | 0 | 0 | 2 | 3 verified, 0 dismissed, all fixed. The placement formula's exact sine and cosine are stated as a departure from the engine's table, and INV-7's port and tolerance pinned; INV-1 is graded against bytes authored from § 4.2; INV-3's fixture gives every excluded actor a `Brush`, so each is excluded by the rule it isolates. One finding filed by one lane as Q4 is counted as Q1, as the other two filed it. |
