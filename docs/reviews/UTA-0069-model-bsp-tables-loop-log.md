# UTA-0069 — `upkg`: the `Model` BSP tables — loop log

The review record for `docs/specs/UTA-0069-model-bsp-tables.md`,
kept outside the spec per `~/.claude/standards/spec-format.md` § 6.
`review-contract` writes one row per loop as it closes. Rows are never
back-filled and a landed row is never edited.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-07 | 3, cold — genre pinned `spec`; packet carried UTA-0004 §§ 2.1/4.1/4.2/4.3/4.5/4.10/7, UTA-0057 §§ 4.3–4.5 and § 5, the complete `Geometry.h`, `Geometry.cpp`, `ByteReader.h`, `Package.h` and `Level.h`, `ByteReader::readIndex`, the tier-3 case names, the `tests/unit/` listing, the ROADMAP bullet and CLAUDE.md's two spec traps | 6 | 4 | 3 | 1 | **Fourteen verified, fourteen fixed; none dismissed. Three collateral, caught by 4b. First gate on this document.** **All three lanes independently found the same defect, and it is the run's most consequential:** INV-4 stated a 99% exact-consumption floor while UTA-0004 § 7 puts `Model` in a zero-refusal set — *"A refusal there fails the tier"* — and `RealInstallTest.cpp` already writes `REQUIRE(result.has_value())` for that set. § 11 said *"No standard is touched"*, so the two rules lived side by side and an implementer wired either a zero-refusal assertion or a rate. The floor was **my own unfinished derivation written into the contract as a permanent tolerance**; the ROADMAP bullet and UTA-0004 § 4.3 both already state the zero-refusal acceptance, so INV-4 now states it too and § 2.2's 98.06% is named as progress rather than a bar. **All three also found the 70-byte claim**, and executing the fix refuted my replacement as well as the original: the all-empty class measures 69 bytes 18 times, 70 bytes 411,969 times and 71 bytes 121,665 times — the one-, two- and three-byte `Polys` index widths — and that run turned up **198 exports at 65 bytes**, which 68-plus-an-index cannot produce and which § 4.6 now carries as a lead toward a version branch. **Two lanes found that § 2.2's headline result cannot settle what it claimed.** With every table empty each array costs one zero byte, so any permutation of the eleven consumes identically: the measurement settles the array COUNT and the scalar positions, not which array is which. Worse, `Vectors` and `Points` are both 12-byte `FVector` arrays, so **no consumption check anywhere in this document can ever separate them** while § 10 claimed the order was checked by INV-1 and INV-4. § 10 now carries a `nothing` row for that pair, and UTA-0007 binds to the names. **Two lanes found the member-naming rule contradicting its own source:** § 3.2 required *"the engine's own member names"* while the struct it extends is camelCase (`polys`, `rootOutside`, `linked`) — so one builder writes `Nodes` and another `nodes`, and UTA-0007 compiles against one. Every member and element-struct name is now spelled out. **Two lanes found a verified fact of UTA-0004 § 4.5 overturned and unrecorded:** its *"second, shorter run of arrays"* is shorter in bytes on a brush model but holds six arrays against the first run's five, so an implementer reading it as a count reads four after `Polys`. § 4.4 and § 11 record the supersession. **One lane found the tier-1 deferral wrong:** § 4.7 said a fixture could not be encoded until § 4.6 closed, when § 4.4 and § 4.5 already state every field the settled tables need — so all default-gate coverage was being deferred for no reason. Tier 1 is owed now, and INV-3 gained the fixture it needs, § 4.7 having listed only two cases while § 7 claimed three invariants covered. **The one Q4 was INV-5's test clause, which could not fail** — it *"counts the classes"* with no assertion, while § 10 called it the only check separating a correct field assignment from an adding-up byte count. That is the exact shape UTA-0004 § 7 records having removed once already. **Three findings were mine, found while verifying rather than by a lane**, and two are the same failure: § 6 cited § 2.2 for a measurement of sizeless exports and again for null `Polys` references, and § 2.2 made neither — the probe skipped sizeless exports uncounted and lumped null with resolved. Re-measured: the install holds **zero** sizeless `Model` exports. The third was the population, *"838 `.unr` packages"*, where the directory holds 838 entries and 837 packages. **Collateral, all three from my own fixes and caught by 4b's sweep rather than by a lane:** two survivors of the rate vocabulary the INV-4 fix removed, and `numSharedSides` added to § 4.1's returned members but absent from § 3.2's enumeration of them. Document grew 411 to 492 lines, which 4a-min flags; the growth is § 3.2's member enumeration, § 4.7's three fixture cases and § 4.6's second residue class, each closing a Q3 gap rather than restating anything. |
