# UTA-0052 — `umat`: a texture memory budget, block compression and a per-material upscale cap — loop log

The review record for `docs/specs/UTA-0052-texture-memory-budget.md`,
kept outside the spec per `~/.claude/standards/spec-format.md` § 6.
`review-contract` writes one row per loop as it closes. Rows are never
back-filled and a landed row is never edited.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-09 | 3, cold — genre pinned `spec`; packet carried the whole of `src/ubundle/Bundle.h`, `src/ubundle/CMakeLists.txt`, `src/CMakeLists.txt` and `src/umap/CMakeLists.txt`, the `ErrorCode`/`Result` and `JobSystem` windows, the root `CMakeLists.txt` floating-point block, `tests/CMakeLists.txt`'s unit target, UTA-0008 §§ 3/4.1–4.4/4.10/5(INV-4..12)/11/13/14/15, `design.md`'s § The parts, rules 1–5 and 15–19, its Content-addressing and Determinism bullets and § The stack, `versioning-overrides.md` § Breaking surfaces, ADR-0007 § Decision, this project's CLAUDE.md § Build and test, and UTA-0049's invariants. `src/umat/`, `third_party/bc7enc/`, both new test files and the vendored encoder's source were declared an unrunnable region up front, as was MSVC | 1 | 5 | 3 | 2 | **Eleven verified, eleven fixed, none dismissed. First gate on this document.** **All three lanes independently found INV-8 ungraded** [Q4]: its *Test* named the root `CMakeLists.txt` guard, which iterates `CMAKE_CXX_FLAGS` and its four per-config variants — cache variables — so the per-target `target_compile_options` its own *Breaks when* names is invisible to it and configures green. § 7 and § 10 both asserted a grading that did not exist. INV-8 now reads `uta_umat`'s `COMPILE_OPTIONS` property in INV-12's `FATAL_ERROR` form, § 4.1 specifies both assertions, and § 7 requires each to be seen firing once. **All three also found `upscaleFactor` unsatisfiable for an over-size source** [Q2]: it had to leave both edges at or below `MAX_OUTPUT_EDGE` *and* return 1 when nothing qualified, while § 4.3 admits a base level to 8192 and UTA-0009 admits a locally-supplied replacement — so a 2048 source made the two clauses contradict, and one reading made § 4.3's derived factor `width / sourceWidth` truncate to 0, destroying the one thing the source dimensions are stored for. Settled as: `MAX_OUTPUT_EDGE` bounds *upscaling* only, 1 is the floor, `umat` never downscales, and the budget is what refuses a 2048 texture that will not fit — a decision somebody sees, where a silent clamp is the degradation § 3 decision 3 rules out. INV-11 gained the 2048 case, which is the only one in its list that separates the two readings. **Two lanes found § 11's UTA-0008 edit list incomplete** [Q2]: it omitted that spec's § 14, whose opening states *"A reader accepts `formatVersion == 1` and nothing else"* — the whole compatibility argument in version terms — so applying the listed edits left the corpus stating two versions, the exact harm § 4.7 names. § 10's check row read UTA-0008 for INV-4 alone and is now `Partial:`, naming the five edits no verb reads. **One lane found the source dimensions ungraded** [Q3]: INV-2 constrained `width`, `height` and `mipCount` only, so a bundle with `sourceWidth` 0 or one larger than `width` decoded cleanly and the promised derivation was a division by zero or an integer 0. INV-2 now requires them non-zero with an exact shared power-of-two ratio, and § 4.9's `compress` refuses the same set so the only writer cannot produce one. **One found the block traversal order unstated** [Q3] — pinned as row-major with edge padding, because `ubundle` treats `blocks` as opaque so a column-major producer and a row-major consumer agree on every byte count and disagree only as a scrambled texture. **One found `bc7enc.c` needs an enabled language** [Q3]: the root `project()` declares `LANGUAGES CXX`, and enabling C would put the vendored translation unit outside the one guard holding INV-6's determinism; it is now compiled as C++ via `set_source_files_properties`. **One found INV-10's ordering clause ungraded** [Q4] — the order is produced by `measure`, which no invariant touched, so a test calling `enforceBudget` alone could never fail on it; split out as INV-14. **Three the orchestrator found while resolving lane open questions, each a real defect:** § 4.1 and INV-12 required `uta_umat` to link `uta_upkg` while § 7 said nothing here reads a package and no § 4.9 entry point takes one, so the assertion would have refused the correct minimal build — dropped, with UTA-0009 named as adding it; INV-4's fixture read as a `TEXS` payload while INV-13 asserts the `compression` byte, which lives in the section descriptor, making that case unreachable — the fixture is now a whole `.utab` at `formatVersion` 2; and § 4.4 attributed a ban on *transcendental functions* to `design.md`, which actually rules out *"no platform maths library in the simulation or the baker"* [Q1] — narrower, and the difference is exactly a table-driven transform an implementer would have been wrongly told they could not write. **One open question resolved clean and is not in the tally:** § 6's claim that UTA-0047 gave `JobHandle` an observable failure is true — `src/core/Jobs.h` declares `JobHandle::failed()`. **4b's sweep found one collateral of the run's own fixes** — INV-11 still asserted the edge bound the over-size decision breaks — fixed in the same loop. Document grew 995 to 1136 lines; every added passage closes a verified finding, and the largest are INV-2's source clauses, § 4.5's over-size paragraph and INV-14. |
| 2 | 2026-09-09 | 3, cold — briefed identically to loop 1, with no list of what loop 1 fixed; packet rebuilt whole from disk and the subject's line count re-measured (1136). Same unrunnable region declared: `src/umat/`, `third_party/bc7enc/`, both new test files, the vendored encoder's source, and MSVC | 3 | 2 | 2 | 1 | **Eight verified, eight fixed, one dismissed. The cap for a spec, and it binds.** **All three lanes found that loop 1's own INV-8 fix would refuse the correct build** [Q2]: the assertion loop 1 wrote matches `-ffp-contract\|-ffast-math\|-Ofast\|/fp:`, and a target's `COMPILE_OPTIONS` property is INITIALISED from the directory property `add_compile_options` sets — so `uta_umat` already carries `-ffp-contract=off`, `-fno-fast-math` or `/fp:precise`, the flags that enforce the numeric contract. Reproduced rather than reasoned: a throwaway CMake project with `add_compile_options(-ffp-contract=off -fno-fast-math)` at the top level returned `TARGET COMPILE_OPTIONS = [-ffp-contract=off;-fno-fast-math]` from `get_target_property` on a library in a subdirectory. The pattern is now the root guard's own, `(-ffast-math\|-Ofast\|/fp:fast\|-ffp-contract=fast)`, and § 4.1 carries the measurement plus the note that the inherited entries are expected. One lane also saw that the break-it-once step could not have caught this, configuration having already stopped. **All three found `sourceWidth`/`sourceHeight` undefined for a resampled replacement** [Q3]: § 4.3 called them the ORIGINAL texture's dimensions while § 9 defers resampling a non-power-of-two replacement to UTA-0009 — so a 1000×1000 replacement resampled to 1024 has ratio 1024/1000, which INV-2 refuses, making the supported path unbakeable. They are now the dimensions of the image handed to `compress`, post-resample and pre-upscale, and § 9 records that the resample factor is therefore not recorded anywhere. **Two lanes found INV-10's no-degradation clause unfalsifiable** [Q4]: `enforceBudget` takes a `BudgetReport` and never sees a texture, so no implementation can drop, resize or re-compress one and the assertion passes for every possible implementation — while § 10 claimed it as `Partial:` coverage. The clause is cut, § 4.6 states the guarantee where it lives (the signature), and § 10's row is now **nothing**, naming both gaps. **One lane found loop 1's INV-11 rewording had become a second rule** [Q2]: it forbade taking an edge above `MAX_OUTPUT_EDGE` that was AT OR BELOW it, so for a 2048 source the clause never fired and factor 4 qualified — 8192 output — while § 4.5 and INV-11's own test row both say 1. INV-11 now restates § 4.5 word for word, and says why paraphrase was the defect. **One found `MAX_UPSCALE_FACTOR` homeless** [Q3]: INV-2 is a `ubundle` decode-time rule using a constant § 4.5 declared in `umat`, and INV-12 runs `umat` → `ubundle` and never back. It now lives in `src/ubundle/Bundle.h` beside `CompressedTexture`, re-used by `umat`, with the reason in § 4.8. **One found § 13's memory bound false** [Q1]: `measure` and `enforceBudget` take a span of every texture, so the whole set is resident before either runs and the peak EXCEEDS the budget on exactly the over-budget map this item exists for — § 13 claimed it bounded. § 13 now says the refusal is post-hoc and the peak unbounded, and the running-total remedy is filed in § 9. **Two the orchestrator found from lane open questions:** § 3 decision 4 routed `bc7enc` to ADR-0007 route 2 on the ground that *"it ships no build system of its own"* — false, its repository root holds a `CMakeLists.txt` (`gh api repos/richgel999/bc7enc/contents`), which builds a demo executable from `test.cpp` and a bundled `lodepng` rather than a library [Q1]; route 2 still applies on its own stated rationale, quoted and verified verbatim against the ADR, and ADR-0007's question 4 is recorded as mis-worded for its owner rather than corrected here. And § 10's version-bump row said *five of § 11's six UTA-0008 edits*, where § 11 lists seven [Q1] — stale since loop 1 added § 14; the row now names the sections instead of counting them. **One dismissed and recorded:** § 4.4's *"one 4×4 block's 16 bytes"* is wrong for BC4, which is 8, and contradicts § 4.2's own table — true, and it changes nothing anyone builds, since every size in the document comes from `bytesPerBlock` or § 4.3's formula. The lane that raised it reached the same verdict and filed it as an open question rather than a finding. **Three lane open questions resolved clean and are not in the tally:** `add_subdirectory(src)` sits after the floating-point block, so § 4.4's *to every target* holds; UTA-0047 exists and is the item that made a job's failure observable; and `bc7enc.h`'s licence coverage is genuinely unsettled — the upstream `LICENSE` enumerates `bc7enc.c` and not its header — so it is filed in § 15 rather than resolved. **One packet defect, mine:** the `src/CMakeLists.txt` window was cut at six lines of a seven-line file, dropping `add_subdirectory(ubundle)`, so all three lanes read a packet that contradicted its own verified-facts list; each reported it as evidence against the packet rather than as a finding, which is the correct reading. **Cap measurements.** Of this final loop's eight verified findings, **five landed on text this run wrote** — INV-8's pattern, INV-11's rewording, the source-dimension clauses, `MAX_UPSCALE_FACTOR`'s reference and § 10's count, each traceable to a loop-1 ledger row; three were in the original draft. That is an **oscillating cap, not a calm one**: each loop is substantially repairing the last, and nothing in the run suggests a third would stop. Against 1c's recorded span — commit 49b47a8, which created the whole document — **all nineteen of the run's verified findings fall inside it**, expected on a first gate where the gate and the audit are one act. Document grew 1136 to 1237 lines. **Ships.** A spec's cap routes to implementation, which exercises the contract against real code; this gate is not re-run on the document as it stands. |
