# UTA-0006 — `unav`: the navigation graph and the event-wiring graph — loop log

The review record for `docs/specs/UTA-0006-navigation-and-wiring-graphs.md`,
kept outside the spec per `~/.claude/standards/spec-format.md` § 6.
`review-contract` writes one row per loop as it closes. Rows are never
back-filled and a landed row is never edited.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-06 | 3, cold — genre pinned `spec`; packet carried `src/upkg/Level.h` and the upkg CMakeLists in full, windows on `Package.h`'s `ObjectReference`, `Properties.h`, `Class.h`, four `docs/design.md` rules and its subsystem rows, UTA-0057 §§ 4.1/4.5/4.6a/6/7/10 with INV-4 and INV-5, UTA-0004's `struct Level` and INV-9, UTA-0005's INV-7 and INV-3a, `LevelExportWriter`, and the UTA-0006 and UTA-0008 roadmap bullets | 2 | 1 | 4 | 0 | **Seven verified, seven fixed, none dismissed.** **Three findings were returned independently by all three lanes**, the strongest agreement this gate offers, and the sharpest is a false claim about a shipped symbol. § 4.2 said `ObjectReference::index()` is an actor's export-table position; `src/upkg/Package.h` says it is the position in the *relevant* table — the import table for a negative reference — and is *meaningless* for a null one, returning 0. An implementer would have written `ref.index()` unguarded, turning every null endpoint into export 0: a well-formed edge to the wrong actor, which § 4.5's discard rule cannot catch because the index it produces is usually a real node. **Measured while verifying it, and the measurement is why it matters**: 0.12% of reach-spec endpoints in the reference install are null — tens of thousands — while none is an import. § 4.2 now guards on `kind()`, § 4.5 names all three discard cases, § 6 gains a row, and INV-2 gains the `Null` fixture that catches exactly this. **The second all-lane finding is a contract that was never written down**: § 4.1 declared `NavNode`, `NavEdge`, `WiringEdge` and `DanglingEvent` and never `NavGraph` or `WiringGraph`, while the rest of the document bound to `NavGraph::nodes`, `WiringGraph::dangling` and a discard count with no member at all — and § 11 calls § 4.1's types the surface UTA-0008 serialises, so two builders would have produced two incompatible bundle sections. Both graph types, a wiring node type and `discardedEndpoints` are now declared. **The third is two index spaces the document supplied without distinguishing**: § 4.2 identifies an actor by export index while `NavEdge::from` indexed the node vector, and the queries' parameter said neither, so a consumer holding the documented identity would get another actor's edges with no error. The queries now take a node position explicitly and `nodeOf` is the declared bridge. **A fourth, from two lanes, was a rate asserted with nothing behind it** — § 7 guarded "the share of reach specs whose endpoints resolve to nodes" with no floor and no measurement anywhere, UTA-0057's figures being `Paths`-to-array rather than endpoint-to-node. Measured: 99.87%, now stated in § 2.1 with its command and a 95% floor in § 7. **One lane alone found a silent narrowing**: `NavEdge::pruned` was a `bool` where `src/upkg/Level.h` keeps `std::uint8_t` and says narrowing it "would be a claim this reader has not earned" — `ubundle` would have fixed a one-bit field for a byte whose range nobody has established. **Another lane alone found a refusal the design walks into**: `readProperties` refuses a class export with `InvalidArgument`, so the wiring scan over every export fails on any map defining its own classes, which UTA-0057 § 4.6 measured as ordinary rather than rare. **The seventh came from an open question rather than a finding** — § 4.3 assigned the node filter to UTA-0005's walk, but `readAncestry` is entered from a class *export* and a map actor's class is an *import*, so a step every navigation node depends on was unspecified; it is now stated, with `tests/real/RealInstallTest.cpp` named as the shipped precedent. **Four open questions resolved clean and are not in the tally:** `Package::name` does return a `std::string_view` (a lane looked it up); `LevelExportWriter` and `TaggedPropertyWriter` do supply every tier-1 fixture; UTA-0057's bullet does record the in-engine ground-truth offer § 14 leans on; and the exact-vs-case-insensitive match question two lanes raised was settled by measurement — folding case resolves **exactly the same events and recovers none of the dangling ones**, so § 4.4's "exact on the name" stands and § 7's floor does not rest on a figure a different rule produced. That result is now recorded in § 4.4, because two independent readers asked it. |
| 2 | 2026-09-06 | 3, cold — identical brief, packet rebuilt from disk with the subject's line count re-measured, and extended with `Package::name`, UTA-0057 § 4.6, `TaggedPropertyWriter` and the shipped `descendsFromNavigationPoint` | 0 | 2 | 5 | 1 | **Eight verified, eight fixed, none dismissed. Cap reached (2 for a spec); the run files its tail and exits. VIOLENT by § At the cap's measure — seven of the eight landed on text loop 1 wrote**, which is 4a-min's pattern at its starkest and is why this document is routed to implementation rather than to a third loop. **The sharpest finding is the run's only Q4 and one lane found it alone: INV-1's test could not falsify INV-1.** It claimed edges are "in range" and its fixture asserted a discard — but § 4.2 says the wrong index space yields *a well-formed edge to the wrong actor*, because the index it produces is usually a real node, so a range check passes on exactly the defect the invariant exists to catch. It is now an identity claim — the node at `edge.from` carries the export index the spec's `start` resolved to — with a fixture that asserts those indices. Loop 1 wrote the *Breaks when* clause that made the mismatch visible. **All three lanes found the same Q2**, and it is the one defect in this loop that pre-dates the run: § 3.2 said unresolvable edges are "returned and marked, never dropped" while § 4.5 dropped and counted them, so an implementer would have added a marked-edge member `NavGraph` does not have and left the counter at zero. § 3.2 now states both halves per graph. **All three also found the same Q3, and it would have shipped a nearly empty graph with no error**: § 4.3's import-to-class step calls the `PackageResolver` *outside* `readAncestry`, and `src/upkg/Class.h` says that resolver may assume lower-cased input — so an unfolded `BotPack` misses `BotPack.u` on a case-sensitive filesystem, every actor of that package fails the descent test, and its reach specs are then discarded as naming non-nodes. On Linux only, which this project treats as first-class. The shipped `descendsFromNavigationPoint` already folds; the spec now says so. **Three more were loop 1's own additions under-specified**: `discardedEndpoints` never said whether it counts specs or endpoints while § 7's rate is endpoint-based (now pinned, with a both-endpoints-bad fixture that is the only one able to tell the two apart); § 4.1 said "each `CMakeLists.txt`" while INV-6 and § 10 named one file and one library (now one directory, two asserted closures); and § 4.7 promised the queries are "a span into storage rather than a search" while no declared member gave a node its run's offset and length (now on both node types). **Two findings were promoted from open questions rather than raised as findings.** § 4.5 called its case list exhaustive at three; a reach spec's endpoints live in serialised data, which `Package::open`'s table validation does not cover, so an out-of-range export index is possible in principle — measured absent across the install, and now the fourth case, because a builder omitting the bound check passes the whole library and reads out of bounds on the first malformed file. And § 7 never said whether its two floors are per-map or install-wide; per-map fails the tier on the maps UTA-0057 § 4.6a names as carrying `Paths` with no reach-spec array, so they are stated as aggregates. **Two open questions resolved clean and are not in the tally:** `UnrealPackageBuilder` does supply every tier-1 fixture — `ClassExportWriter::setSuperField` for the class chain, `TaggedPropertyWriter::addName` for `Tag`/`Event`, raw `std::int32_t` endpoints for the null and import cases — now recorded in § 7 because two lanes asked; and `upkg` genuinely exposes no import-to-class call, its `importPackageName` being file-local to `Class.cpp`. **One mechanical fix a lane spotted and correctly declined to file**: two pointers to a § 15 that the early renumbering had removed. **Routing at the cap:** the tail is empty — every finding was fixed — and this run's fifteen findings all fall inside the span that armed the gate, since the gate was armed by the document's own creation. So this run was a gate rather than an audit, which is expected for a first spec. The next reviewer is the build. |
