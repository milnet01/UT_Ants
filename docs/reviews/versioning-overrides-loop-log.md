# `versioning-overrides.md` — loop log

The review record for `docs/standards/versioning-overrides.md`, kept outside
the document per `~/.claude/standards/documentation.md` § 9.1, which is the
pattern this project already uses for its specs. `review-contract` writes one
row per loop as it closes. Rows are never back-filled and a landed row is
never edited.

**The document was ungated before 2026-09-08.** That is a gap in the record,
not evidence that it was reviewed and passed.

Its sibling `docs/standards/workflow-overrides.md` was gated later the same
day; its rows are in `workflow-overrides-loop-log.md`. This paragraph said
that sibling was still ungated, which stopped being true within hours of
being written.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-08 | 3, cold — genre pinned `standard`, so Q4 was not asked; packet carried versioning.md §§ 3, 4 and 9, discovery.md's twelve signs, design.md's § The bundle and rules 15–19 and § Close calls, the ROADMAP section counts, and the verbatim roadmap bodies of UTA-0006 and UTA-0007 | 1 | 2 | 3 | n/a | **Six verified, six fixed, none dismissed. THE RUN ENDED HERE ON THE USER'S INSTRUCTION -- "No more loops after this" -- not at convergence and not at the cap, which for a standard is 3.** A single cold read is a weaker gate than a converged one and this row is the only place that difference is recorded; do not read this document as having converged. **The gate was on one new section, `## Which release an item belongs to`, added to record why `umap` and `unav` are 0.1.0 work when their consumers are 0.4.0.** Four of the six findings landed on that new text, which is expected rather than oscillation: loop 1 is its first reading and there was no previous loop to repair. **All three lanes independently found the same two defects.** The first: the section said a release is cut on "the signs of success its row names and on nothing else", and the table it governs does not consist only of signs -- `0.5.0`'s row names none at all, and `0.2.0` and `0.3.0` each add a non-sign clause, so a conformer would have cut `0.3.0` with no Deathmatch and no LAN chat, and could never cut `0.5.0`. It now reads on the cut condition the row states, signs and any further condition in the same cell. The second: the whole rule was keyed on whether a format "this release freezes" must carry the work, and **nothing in the document says which release freezes which format** -- while § Override's own cost clause allows a breaking format change later in `0.x` provided the changelog announces it, which supports the opposite reading. Two conformers would file one item in two different releases, and that divergence is durable because no `roadmap_log` op moves an item between sections. The trigger is now the cost the worked example actually uses -- whether adding the work later would invalidate content already baked or published under one of this release's formats -- chosen over tagging each of the eight breaking surfaces with a freezing release, which would have been eight new claims written on the last loop with no read after them. **One lane found the new section contradicting ITSELF, and it is the sharpest finding of the run:** its opening paragraph says format-carried work lands in the earlier release and that deferring it means re-baking every map, while its closing paragraph said an item the row does not reach is no reason to hold the release -- so a conformer could cut `0.1.0` on S1 and S7 alone with the bundle format still missing the sections `umap` and `unav` fill, which is precisely the cost the section exists to prevent. It now states that exactly two things hold a release. **One lane found the deferral sentence written descriptively**, so a conformer who defers an item and writes no note had breached under one reading and complied under the other; it is now an obligation with an owner. **One lane found § Breaking surfaces' closing exemption ambiguous** -- "the shape of a bundle's *contents* where the format version already covers it" against the requirement that a bundle-format change be announced, where design.md rule 17 makes any new room attribute a format bump. Under the wrong reading a room attribute ships with no `### Changed` entry and every operator re-bakes a rotation unwarned; the exemption is now from being listed separately, never from being announced. **One finding was mine, from resolving a lane's open question:** the document cited `releases.md` § 2 for "a release whose section contains one leads with it", and `releases.md` contains no such rule anywhere -- grep for "lead" over that file returns nothing. The instruction is kept and now owns itself as this project's addition rather than citing a source that does not carry it. **Resolved clean, not counted:** the exit condition requires both S6 and S8 where discovery.md calls S8 the last sign -- two lanes judged that redundant rather than contradictory and neither filed it. **Left open, surfaced rather than fixed:** whether any of the seven items deferred out of 0.1.0 falls in this section's own format-carried class. Several would add material parameters, and the bundle's material sections are defined by UTA-0009 and UTA-0010, which are themselves 0.1.0 work -- so the question is not live today and becomes live the moment those land. 113 to 148 lines. `doc_integrity` clean over both files; `doc_citations` checked 0 quotations, which is silent about this document rather than a pass, its quoted fragments being italicised rather than double-quoted. |
