# UTA-0057 — `upkg`: the rest of `Level`, and the ReachSpec path graph — loop log

The review record for `docs/specs/UTA-0057-level-tail-and-reachspecs.md`,
kept outside the spec per `~/.claude/standards/spec-format.md` § 6.
`review-contract` writes one row per loop as it closes. Rows are never
back-filled and a landed row is never edited.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-06 | 3, cold — genre pinned `spec`; packet carried UTA-0004 §§ 4.1/4.3/4.5/4.9 with its INV-1 and INV-9, § 7's tier structure, Geometry.h in full, Properties.h, Package.h, the upkg CMakeLists, four ROADMAP bullets, the consumer's Paths evidence file and CLAUDE.md's traps | 1 | 4 | 5 | 0 | **Ten verified, ten fixed, none dismissed. Two collateral. Three open questions resolved clean.** **All three lanes independently found the same defect**, the strongest agreement a three-lane loop offers: § 6 made an out-of-range `Paths` value *always* a layout defect while § 4.6 admitted it might instead falsify the indexing claim, in which case the item still ships — two different test files and two different item outcomes from one observable. § 4.6 now names a single disposition and § 5 makes INV-2/INV-3 explicitly conditional, withdrawn in the fold-back rather than left failing. **The Q1 was mine, copied from a source whose own table contradicted it.** § 2.2 said MH-AncientCavesTorus had the largest mismatch and no node at the cap, and that MH-Dust2-BP was the only map where the cap binds. Re-measured: MH-Village1 has the largest residue (111 vs 72) and one capped node, so both halves were false. The conclusion survives on the case that actually carries it — AncientCavesTorus shows a residue of 72 with zero capped nodes, and truncation cannot drop what was never truncated — and the residue and cap counts are now columns of the table so the elimination is checkable from the spec instead of from a foreign file. Two lanes found it; the third raised it and declined to file it, judging the conclusion unaffected. **The sharpest finding was measured rather than argued.** A lane asked how a `NavigationPoint` is identified; measuring the three maps showed that **no export of literal class `NavigationPoint` carries a `Paths` entry at all** — every one is a subclass (`Spawnpoint`, `InventorySpot`, `PathNode`, `Teleporter`, `VisibleTeleporter`, `PlayerStart`, and the map-defined `QueenDest`). An exact-name filter resolves zero and trips the fail-on-zero guard added that morning, and a hand-written list cannot be complete while maps define their own subclasses. The filter now walks ancestry and **UTA-0005 joins Blocked by**. **A second lane found the returned type cannot express what it may have to return:** `actors` drops null slots and keeps only their count, so if a `ReachSpec` names its nodes by raw slot index there is nothing to join against — § 4.1 now requires the slot mapping in that branch, since UTA-0006 binds to this type. **And the header was quietly wrong about its own dependency:** it said UTA-0004 shipped the actor array, but `readLevel` was declared there and never built (`grep -c Level` over PackageContentTest returns 0), so this item is the first to implement it and inherits INV-9's *untested* obligation — now in § 4.2, § 7 tier 1 and § 10. **Two open questions were promoted to findings** rather than answered: whether `Level` joins UTA-0004 § 7's zero-refusal set (it does, and this item decides it), and whether § 4.6 settles over one map or the install (the install, or the shipped assertion rests on a narrower check than the one that settled it). **Also fixed:** INV-2 asserted a non-zero count while § 7 asserted an 8,511 floor — one guard, two builders; and § 7 called this reader's position the same as `readModel`'s when § 4.7 grows it a fixture, which UTA-0004 § 7 explicitly says `readModel` has not. **Collateral from the fixes, caught by the 4b sweep:** § 10's recount and § 13's cost, which priced the ancestry dependency and the owed INV-9 fixture at zero. **Resolved clean, not findings:** `Property` carries `arrayIndex`, so the 16 slots are observable and § 2's premise holds; `Package::open`'s range check does precede every table read; and UTA-0005's INV-3a is a real declared-reading-check precedent for § 7 tier 2. |
| 2 | 2026-09-06 | 3, cold — identical brief, packet rebuilt from disk with the subject's line count re-measured | 3 | 5 | 0 | 1 | **Nine verified, nine fixed, none dismissed. Cap reached (2 for a spec); the run ships.** **A VIOLENT cap: six of the nine landed on text loop 1 itself wrote**, checked against loop 1's ledger rather than recalled. Every finding of the whole run falls inside the gated span, this being a new document. **All three lanes found the same defect, and it was loop 1's own fix.** Loop 1 closed a gap by saying a `Paths` slot is empty when it holds "the file's own unused-slot sentinel, and § 4.5's derivation is what identifies that value" — but § 4.5 derives the reach-spec array in the `Level` tail, and the slots are tagged properties on the actor, so the section named could never supply the value. Two lanes supplied the right answer from the packet: `Property` carries `arrayIndex`, so an unused slot is an ABSENT property, not a sentinel. The rule now says so and names the observation that would falsify it. **Two lanes found a command that does not produce its own table.** Loop 1 added cap columns documented as `grep -c 'Paths(15)='`, unanchored — which matches inside `upstreamPaths(15)=`. Measured: it returns 155, 68 and 298 where the table records 0, 0 and 7, so anyone re-deriving the table concludes the table is wrong. The anchor is restored and the reason stated, since § 11 makes re-running this a check on `ut-dump`. **Three lanes between them found § 11 recording pointer corrections while missing the amendments this item actually forces on UTA-0004**: `struct Level` gains a member in the document that DECLARES the type for four binding items, § 7's zero-refusal list gains `Level`, and § 10's INV-1 and INV-2 rows go false because § 4.7 builds the fixture they say does not exist. An implementer following § 11 would have edited two pointers and left the owning document superseded. **Loop 1's fixes also collided with each other twice:** the slot-to-actor mapping § 4.1 requires on one branch is a "map keyed by node", which INV-5's own test clause forbids — so one build fails tier 2 and the other leaves UTA-0006 unable to join anything; and § 7's new zero-refusal claim swallowed UTA-0004's two recorded-rather-failed shapes, one of which § 6 still keeps in a row of its own. **The one Q4 was pre-existing and is the sharpest thing about the item's honesty:** a `ReachSpec`'s collision radius and height are promised in § 1, returned in § 4.1, bound to by UTA-0006 — and no invariant reaches them. Transposed, they consume the same bytes and name the same nodes. § 10 now grades them `nothing` outright. **Two more pre-existing:** the T3D corpus path moved mid-run and the commands cited it, and § 4.7 implied a `Level` fixture exists to extend when `UnrealPackageBuilder` emits none. **Resolved clean:** UTA-0005 § 4.6 does resolve a parent in another package, so § 4.6's ancestry filter is viable. **Shipping at the cap with an empty tail.** Every finding is fixed, but the collateral rate says the next reviewer should be the build rather than a third cold read: two of the run's most expensive findings were repairs of repairs, and both were caught only by running something rather than reading it. Per § At the cap this document is not re-gated as it stands — an authoring edit that changes direction re-arms it normally. |
