# ADR-0008 — acquire dependencies by route — loop log

The review record for
`docs/decisions/ADR-0008-acquire-dependencies-by-route.md`, kept outside the
ADR. `docs/decisions/README.md` gives an ADR no loop-log section and says it
is never edited after acceptance, so rows cannot go in the document.
`review-contract` writes one row per loop as it closes. Rows are never
back-filled, and a landed row is never edited.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-13 | 3, cold — genre pinned `adr`; packet: `docs/decisions/README.md`, ADR-0007 whole, ADR-0006's header, ADR-0002's reproducibility sentence, **S7**, `docs/design.md`'s `ubundle` row and § The stack, the new standard whole, the `find_package` and glm CMake blocks, `ci.yml`'s Vulkan steps. Unrunnable region: the runner package census and upstream repository contents, judged against ADR-0007's record | 1 | 2 | 1 | n/a | **Four verified, four fixed, none dismissed.** All four anchor in the gated span, commit 71500fb. **[Q1] glm was justified as arithmetic the baker runs**, while only `uta_urender` links it; the pin is now grounded on what ADR-0002 would require once baker arithmetic uses it. **[Q2] Two lanes found *"Two things have no mechanical check"*** against a standard whose What-checks-this table marks more; the bullet now points at that table rather than counting. **[Q2] The standard's routing question 2 had dropped ADR-0007's test** and read *a* route-3 acquisition, so a library only the SDK ships would route differently from the decision; fixed in the standard, which owns the question. **[Q3] ADR-0007's obligation that the README name the Vulkan prerequisite was carried nowhere**; the standard's § 5 now states it. **Clean-resolved open questions:** `README.md` already names the inputs and both acquisitions; ADR-0007's placement of the `find_package` binds nothing now that it is superseded; the vcpkg validation-layers port is in ADR-0007's record. **Collateral:** route 4's wording, tightened after the standard's question 5 changed. |
| 2 | 2026-09-13 | 3, cold — briefed identically; packet rebuilt whole from disk with the subject re-measured, adding `docs/design.md`'s runtime-target definition and the facts that only `uta_urender` links glm and that the README names the Vulkan inputs | 0 | 2 | 0 | n/a | **Two verified, two fixed, none dismissed. All three lanes found both.** **[Q2] Route 4 — loop 1's own collateral edit — read *the one optional tool*,** which every tool behind `UTA_BUILD_TOOLS` satisfies while the standard's question 5 excludes them; it now reads a tool behind a build option of its own. **[Q2] Route 1 kept ADR-0007's version-matching test** after the standard's question 1 had moved to the platform's driver stack, and the Supersedes line said the decision was unchanged; route 1 is now worded in the standard's terms, and the Supersedes line says the routes and each named dependency's route are unchanged, with the routing question moved. **One of the two landed on text loop 1 wrote**; the other was draft text a loop-1 fix to the standard put out of step. **Clean-resolved open questions:** `UTA_BUILD_TOOLS` exists and guards `tools/`; no bundle section carries shader-derived data by § The parts' list. **Collateral, from the 4b sweep:** `docs/design.md`'s index prose, as recorded in the standard's row. |
| 3 | 2026-09-13 | 3, cold — briefed identically; packet rebuilt whole from disk with the subject re-measured, adding the `UTA_BUILD_TOOLS` option and its guard on `tools/` | 1 | 0 | 0 | n/a | **One verified, one fixed, none dismissed. All three lanes found it. Cap reached (3 for an ADR); the tail is empty and the run exits.** **[Q1] The Supersedes line said the four routes were unchanged**, while routes 3 and 4 had been narrowed during this run — route 3 from ADR-0007's version-matching test to the platform's driver stack and its Vulkan toolchain, route 4 from one non-runtime target to one tool behind a build option of its own — so a reader sent back to ADR-0007 by that sentence would guard a fetch the standard leaves unguarded. The line now keeps each named dependency's route and states both narrowings, and ADR-0007's status line, which said ADR-0008 *keeps this decision*, says the same. **Cap measurements.** The final loop's one finding landed on text this run wrote (loop 2's rewrite of the Supersedes line): a share of one in one. The run's verified findings went four, two, one, on a short document, so the verdict taken is a **calm** cap: the share is a single finding, and the counts fell every loop. **Against the gated span, commit 71500fb, five of the run's seven verified findings anchor on text as drafted there**; the other two anchor on this run's own edits. **Clean-resolved open questions:** ADR-0007's README deadline before `0.1.0` is carried by the standard's § 5 obligation, which `README.md` already meets; the platform set in the Context omits the validation layers, which ADR-0008 decides nothing about. |
