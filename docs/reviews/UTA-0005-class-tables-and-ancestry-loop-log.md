# UTA-0005 — `upkg`: class tables, default properties and ancestry — loop log

The review record for `docs/specs/UTA-0005-class-tables-and-ancestry.md`,
kept outside the spec per `~/.claude/standards/spec-format.md` § 6.
`review-contract` writes one row per loop as it closes. Rows are never
back-filled and a landed row is never edited.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-05 | 3, cold — genre pinned `spec`; packet carried UTA-0003 §§ 4.3-4.8, UTA-0004 §§ 2.1/4.1/4.3, ADR-0003, ADR-0004 and windows on Properties.h, Package.h, ByteReader.h, Error.h, the upkg CMakeLists and the census script | 3 | 1 | 5 | 4 | **Thirteen verified, thirteen fixed, none dismissed. Four collateral.** **All three lanes independently found the same defect, and running it proved the document worse than they could see.** § 2.1 claimed the export table's `super` column is sometimes out of range so the class's own `SuperField` must be preferred; every lane noticed the sentence contradicts itself, since UTA-0003 § 4.6 has `Package::open` validate both tables, so such a package could not open at all. Re-running the measurement showed the claim was simply FALSE: the probe accumulated the compact index without narrowing to 32 bits, where `ByteReader::readIndex` narrows once at the end, so a five-byte encoding with bit 31 set read as a large positive instead of a small negative. With the narrowing, zero class exports in the install have an out-of-range parent in either place. § 4.5 now takes the parent from `ExportEntry::super` BECAUSE the container has already validated it, INV-2 is inverted, and § 2.1 records the probe defect — the probe is this item's only oracle, so a lesson about it is worth more than the finding was. The same bug was in the committed `scripts/class-census.py` and is fixed; its numbers are unchanged, which confirms it never touched the ancestry walk. **[Q3] `AncestryEnd::Truncated` had no producer anywhere** (all three lanes) while § 6 routed 'parent's class absent from a package that opened' to `PackageMissing`, whose own field comment says `missingPackage` is set — so a caller would report a package missing that is present, the opposite of ADR-0004's legibility requirement. The enumerator is now `ClassMissing` and owns that case, with `missingPackage` empty in it. **[Q4] INV-4's fixture could not fail against the defect it exists to catch** (lane 1): with ONE instruction a walker charging eight memory bytes instead of four consumes the same disk bytes and stops at the same literal; three instructions at `ScriptSize` 15 is the smallest fixture that discriminates. Lane 3 asserted the opposite and was wrong. **[Q3] The walk's overshoot was unspecified** — and § 2.1's own fourth finding says the exact-consumption rule cannot catch a wrong script end, so one builder's `while (total < size)` would ship the silent failure this item exists to prevent. **Two more test clauses claimed more than they exercised:** INV-8 stated three rules and isolated one, and INV-3 stated 'executes nothing' and tested only the opcode refusal — split out as INV-3a, a declared reading check, since no fixture can demonstrate the absence of an interpreter. **A lane open question became a finding on measurement:** every class export in the install is at package version 68 or 69, so § 4.3 step 10's version-62 branch is never taken by real content and INV-1 says nothing about it; § 7 now names it as fixture-only. **Also fixed:** the dependency and package-import lists did not state their encodings, in a document whose every other reference does; the merge key left case unsettled in the one paragraph about a merge that silently fails to override; and § 10 credited the census script with guarding all of § 2.1's numbers when it exits non-zero on exact consumption alone. **Collateral, from the 4b sweep:** § 2's 'wrong in four ways', INV-7 naming one end state after § 6 routed two to it, the merge paragraph naming one incomplete end, and § 3.3's summary. |
| 2 | 2026-09-05 | 3, cold — identical brief, packet rebuilt from disk and its census-script window refreshed | 0 | 4 | 3 | 2 | **Nine verified, nine fixed, none dismissed. Cap reached (2 for a spec); the run ships.** **Not one Q1** — every defect was two passages disagreeing or a rule a builder could not follow. **Five of the nine landed on text loop 1 wrote, which is a high share and is recorded as such: this run was partly oscillating rather than calm.** The clearest case is two loop-1 fixes colliding. Loop 1 added *the total must land on `ScriptSize` exactly*, and separately rewrote INV-4 to demand a three-instruction fixture because *with one instruction the disk position is identical*. Under the exactness rule that is false — one instruction at `ScriptSize` 5 already overshoots at 9 — so INV-4's own rationale described a walker that stops when the total *reaches or exceeds*, which is the lenient reading the exactness rule exists to forbid. All three lanes found it; running the arithmetic settled it. INV-4 is now one instruction, asserting success and a literal position. **All three lanes also found loop 1's negative-`ScriptSize` rule unreachable**: the published signature takes `std::uint32_t`, so `skipScript` can never see a negative value and INV-3's third fixture could not be written against it. The parameter is now `std::int32_t` and the refusal is `skipScript`'s first act. **And all three found loop 1's INV-3a listed among fixture coverage** in § 7 while the invariant itself says no fixture can demonstrate the absence of an interpreter. **The run's sharpest finding was pre-existing and is one lane's alone:** § 4.4 stated the memory width of operands and never of the opcode byte, and the whole walk turns on it — an instruction carrying one object reference costs five, not four, so a builder charging operands alone computes 12 where the file says 15, and INV-1 then fails across every scripted class with no clue why. **Two more pre-existing Q3s, each a rule with no owner:** nothing said whether `readAncestry` or the injected resolver folds a package name, and the resolvers are other items' — so one side folds, the other does an exact lookup, `botpack` misses `BotPack.u` on Linux, and an installed mod is reported absent, which is the failure INV-7 exists to prevent; and § 4.7 never said which spelling of a merged property name survives, in a document whose consumers compare that field. **One lane found the fixtures unbuildable:** § 4.9's builder could not write an in-data `SuperField` differing from the export column, a negative `ScriptSize`, or a chosen name-table index — the three fixtures loop 1 had just required. **A fix of my own was caught by 4a step 3 before it landed**: settling a lane's open question about the depth cap, I wrote that `scripts/class-census.py` re-derives the depth, and it did not; the script now reports it (deepest chain reaching a root: 12) rather than the claim being deleted. **Open questions settled by measurement, not argument:** two lanes doubted that every class export sits at version 68 or 69, since the packet's fact covered `.u` files alone — re-counted across `.unr`, `.utx`, `.uax` and `.umx` too, it holds, and § 7 now says so and names the script. **Shipping at the cap:** the tail is empty, every finding is fixed, but the collateral share is high and § At the cap's routing applies — the next reviewer is the build, not a third cold read. Both underived areas from UTA-0004 stay out of scope; what this document has not been checked against is a compiler. |
