# ADR-0007 — acquire dependencies by route — loop log

The review record for
`docs/decisions/ADR-0007-acquire-dependencies-by-route.md`, kept outside the
ADR. `docs/decisions/README.md` fixes an ADR's shape and it has no loop-log
section, and says an ADR is "never edited after it is accepted" — so the row
cannot go in the document itself. `review-contract` writes one row per loop as
it closes. Rows are never back-filled and a landed row is never edited.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-06 | 3, cold — genre pinned `adr`; packet carried § The stack as edited, design rule 2 verbatim, S7 verbatim, ADR-0002 and ADR-0003, the Catch2 fetch, `ci.yml`'s matrix, the README Install section, and a Verified source facts block holding an `ubuntu:24.04` package census, upstream release tags, shaderc's `third_party/CMakeLists.txt` and the `windows-2022` runner manifest. The unrunnable region — MSVC and any Windows build behaviour — was stated up front, so Q1 was out of scope there | 1 | 2 | 1 | n/a | **Five verified, five fixed, none dismissed. Two collateral.** **All three lanes independently found all four of their findings — total agreement, which no previous gate in this project has produced.** **The sharpest is that the document's central device did not reproduce its own decisions.** The question a new dependency is asked opened *"does it contain or dispatch into a system component — a driver, a kernel interface, a device?"*, and SDL3 opens input, audio and window devices, so the question routes it to "required from the platform, found and never fetched" — which the Context table proves unbuildable, SDL3 being absent from `ubuntu-24.04` and from `windows-2022` alike. The same question routed `shaderc` to route 1, the one route the document had just shown impossible, and Catch2 to route 4. An ADR whose stated purpose is that the answer be "mechanical rather than re-argued" was mechanically wrong about three of its own five entries. The test is now *matching* rather than dispatching — must its version match something already installed — with an explicit branch for a tool a route-3 acquisition already carries, and a stated exception keeping a test-only dependency at route 1. **[Q1] The document contradicted the edit it shipped with, about a sign of success.** It said **S7** "now reads 'clone, install one SDK, build' rather than 'clone, build'", where S7's own text is about a stranger *with no Unreal Tournament on their machine* and says nothing about a toolchain — and `docs/design.md`, edited in the same commit, said the SDK step "is the one step **S7** does not cover". A builder maintaining the S7 check would have rewritten the sign on this document's authority. **[Q3] The one thing route 3 forbade diverging was the one thing it never pinned.** "What must not diverge is which SDK the build then finds" named no version, no floor and no artefact — and on Linux "the Vulkan SDK" is not one thing at all: the loader, the layers and `glslc` are three distro packages, Ubuntu's `glslc` being `2023.8` against the SDK's own much later build. `glslc` emits SPIR-V and SPIR-V feeds a bundle hash `ADR-0002` requires equal across machines, so two compilers is the glm defect one step further down the pipeline. Route 3 now names the LunarG SDK at a `1.3.275` floor — the version Ubuntu 24.04 packages, so the floor is not above the older platform. **[Q2] A consequence forbade in its heading what its own last sentence prescribed**, `ci.yml` being ruled out as the sole home of SDK acquisition four lines before provisioning was assigned to it; split now into installation in the workflow and assertion in the shared gate script. **The fifth finding was the orchestrator's, from a lane's open question rather than from a lane.** Two lanes flagged the vcpkg rejection as resting on an unmeasured claim, and measuring it showed the claim false: only vcpkg's `vulkan` metaport is find-only, while `vulkan-loader`, `vulkan-headers`, `shaderc` and `glslang` are real ports that build from source. The rejection now rests on cost — a tool and a manifest in every contributor's loop — and says so, since the capability argument is the tempting one and it is wrong. **Collateral, both from the 4b sweep and both this change's own text, hours old:** the Context still called a fetched Vulkan loader "wrong by construction", which the vcpkg measurement had just falsified; and `docs/design.md`'s pointer paragraph still stated the *dispatching* test the Decision had narrowed to *matching*, so the two documents disagreed about the rule one of them says the other owns. **Open questions resolved clean, none a finding:** `0.1.0` has not been cut (`CHANGELOG.md` carries only `[Unreleased]`, and there are no tags), so "before `0.1.0` ships" is reachable; and the runner manifest carries no SDL, glm, Assimp or `glslc`, so the table's "absent" column is right where the packet had only verified Vulkan. |
| 2 | 2026-09-06 | 3, cold — identical brief, packet rebuilt from disk with the subject re-measured at 163 lines, plus the vcpkg port census and the release state loop 1's open questions had needed | 1 | 3 | 2 | n/a | **Six verified, six fixed, none dismissed. One collateral.** **Four of the six landed on text loop 1 wrote, which is this phase's measured pattern at its clearest — and the sixth, found while verifying rather than by a lane, is the premise the other three rested on.** **All three lanes independently found that a floor is not a pin**: loop 1 set route 3 at "1.3.275 or newer" inside a paragraph promising "One acquisition, one version, both platforms", so two contributors could satisfy the floor with different compilers, and route 1 pins glm to an exact tag for exactly the reason the floor waives. Two lanes added that the floor was *derived from* the Ubuntu packages the next paragraph rules out, so a Linux box could meet the floor by the acquisition the document rejects. **Verifying that knot showed the premise under it was false, and no lane could have reached it — the packet had no window on what a bundle contains.** Loop 1 wrote that "SPIR-V goes into a bundle whose hash `ADR-0002` requires to be equal across machines". `docs/design.md`'s `ubundle` row lists a map bundle's contents — geometry, materials, collision, lights, baked indirect light, entity placements and the graphs — and compiled shaders are not among them; they build into the engine binary, and nothing in the design has the baker running shaders. So ADR-0002's hash requirement never reached `glslc`, the determinism argument forcing one version was unfounded, and removing it dissolved the floor-versus-pin contradiction rather than papering it. Route 3 now states four components at Vulkan 1.3 or newer, says outright that the distribution's packages qualify on Linux, and says why a floor is correct — nothing it supplies enters a bundle — while noting that glm is not the same case, being arithmetic the baker runs whose results do. **[Q3] Two lanes found the gate's assertion had no named observable**, with `$VULKAN_SDK`, a system path and two unrelated version schemes all available and the document choosing none; it now reads what CMake reads, `find_package(Vulkan)`'s `Vulkan_VERSION` and `Vulkan::glslc`, so the gate, CMake and the README cannot each pick differently. **[Q3] Two lanes found the routing question's second limb answered nothing** for a dependency unbuildable on its own that no route-3 acquisition carries — it said "route 3, on that acquisition's back" where no such acquisition exists. Split into its own question answering route 2, vendored with its synced siblings, which is the branch `shaderc` would take. **[Q2] One lane filed, and two raised as open questions, a Context claim loop 1's own sibling edit had made false**: the five dependencies "have no stated answer" in design.md, which now carries an acquisition column for all five. Loop 1's lane 1 had dismissed the same observation as immaterial; on a second appearance from a different lane it is fixed in the past tense, naming this change as what added the column. **Collateral, both from the 4b sweep:** the Consequences still required "installing the Vulkan SDK" on both platforms after route 3 admitted the distro packages on Linux, and `docs/design.md`'s pointer still said the SDK carries its parts "as one set at one version", which is the over-claim this loop removed. **Open questions resolved clean, none a finding:** no lane had a window on `UTA-0049-numeric-contract.md` or `local-gate.md` § 3, and neither bears on a fix made here.
