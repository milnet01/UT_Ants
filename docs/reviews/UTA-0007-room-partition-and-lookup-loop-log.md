# UTA-0007 — `umap`: partition a level into rooms, and answer which room a point is in — loop log

The review record for `docs/specs/UTA-0007-room-partition-and-lookup.md`,
kept outside the spec per `~/.claude/standards/spec-format.md` § 6.
`review-contract` writes one row per loop as it closes. Rows are never
back-filled and a landed row is never edited.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-08 | 3, cold — genre pinned `spec`; packet carried design.md's subsystem rows and rules 2/17/18, UTA-0006 §§ 4.1/4.6, UTA-0069 §§ 3.2/11, the whole of `Geometry.h`'s element structs, `Properties.h`'s `Vector3`, `src/unav/CMakeLists.txt`, the real-asset harness, `ci.sh`'s step harness and the ROADMAP bullet; the UE1 wiki conventions were declared an unrunnable region up front | 3 | 3 | 3 | 2 | **Eleven verified, eleven fixed, one dismissed. First gate on this document.** **All three lanes independently found the same two defects, and both were structural.** The first: § 4.4 step 1 said to take a room's extent "from the leaves that name it", and `Leaf` carries `iZone`, `iPermeating`, `iVolumetric` and `visibleZones` — **no geometry of any kind**. The step was unexecutable, and `Room::minZ`/`maxZ` and all of § 4.5's banding hung off it. § 4.4 is rewritten as ONE lattice pass over `Model::boundsMin`/`boundsMax` bucketed by room, so extents fall out of the pass instead of being an input to it; that also made the cost independent of room count (§ 13 corrected, it had said per-room). The second: **INV-2 was circular and could not catch the defect it exists for.** It asked that "a point inside that leaf" resolve to that leaf's zone, and with no leaf geometry the only way to obtain such a point is the § 4.3 descent — so the test asserted the descent against itself and a consistently swapped front/back convention passes it, while § 10 names it the sole catcher of exactly that. INV-2 now probes off a node's own polygon centroid and compares against **that node's own zone record**, which is read rather than descended. **All three also found the room-creation ambiguity:** INV-1 read as one room per zone-table entry while § 4.2 concluded 26 maps yield one room — and those maps have multi-entry tables, so the two rules disagreed by up to 63 rooms on the same map, which is the shape `ubundle` serialises. The rule is now stated outright: a zone gets a room only where a leaf names it. **Two lanes found the census arithmetic:** 837/836 walked, 834 with more than one zone entry, 0 with exactly one — leaving 2 maps with an EMPTY zone table, present in the measurement and absent from the table and from § 6. Both added. **Two found undeclared types** — `RoomBuildOptions` and `Point3` were used and never declared, and `simplifyTolerance` was given a relative default, so two conforming builders would disagree on it from the same document; all three are now declared with absolute defaults. **Two found § 6 promising a report channel that did not exist**, `Result<RoomMap>`'s error arm being for refusals; `RoomBuildReport` now rides the success path. **One lane found the disconnected-zone case** — marching squares on a zone occupying two pools yields two outer rings and `Room` held one `outline`, so `Room` now carries `parts`. **One found INV-2's per-leaf assertion contradicting § 7's "a population and a rate"** with no threshold; the tier now asserts the hard form, since a swapped convention is wrong at every probe and any threshold below 100% passes it. **One found INV-4's test name spelled differently in § 5 and § 10** — this project's own notes record a Catch2 filter matching nothing reading as a pass. **Two were mine, from resolving lane open questions:** INV-6 turned on an implementer's `>` versus `>=` at a one-cell room, fixed by forbidding the simplifier to reduce a ring below three vertices; and INV-7's rationale claimed a doc comment in § 4.1 that is spec prose. **Dismissed, recorded:** § 2 attributes "a bit per player per room" to design rule 18 where it sits in the *Exploration is per-room* bullet — true, and nothing is built differently. **Resolved clean, not a finding:** § 8's "610 maps" against § 4.2's 837 — different populations (the server's rotation versus the install's `Maps` directory), now said so in place because two lanes stopped on it. **Collateral, caught by my own 4b sweep rather than by a lane:** § 13's per-room sampling cost, a `§ 15` cross-reference to a section that does not exist, and two `outline`/`footprint` vocabulary survivors. Document grew 531 to 663 lines, which 4a-min flags; the growth is the type declarations, the room-creation rule and § 4.4's rewrite, each closing a defect rather than restating anything. |
| 2 | 2026-09-08 | 3, cold — same brief, same packet, rebuilt from disk; no lane was told what loop 1 fixed | 2 | 5 | 2 | 0 | **Nine verified, nine fixed, none dismissed. THE CAP, AND IT IS A VIOLENT ONE: seven of the nine landed on text loop 1 itself wrote.** Anchors checked against loop 1's ledger rows rather than recalled. Each loop repaired the last and nothing in the run suggests a third would stop, so under § At the cap this ends the REVIEW of the document as it stands — not the shipping. The spec takes its accepted status, the tail below is filed, and it routes to implementation, which exercises the contract against real code and is the better third reviewer. **Loop 1's own repair of INV-2 was wrong, and two lanes found it from different directions.** The invariant probed every node with a vertex pool against that node's `iZone`/`iLeaf` record — but § 4.3 consults that record ONLY where the descent stops, so on a side with a child the test compared the answer against a record the descent never reads, and § 7's hard form would have gone red on correct code. Probing is now restricted to sides whose child is `INDEX_NONE`, which is still exactly where a swapped convention misroutes; and a record naming zone 0 or a zone no leaf occupies must return `NO_ROOM`, counted as agreement, because § 4.2 creates no room for those. **Loop 1 also left the descent unbounded in RANGE while bounding it in TIME**, found by two lanes: the iteration counter stops a cycle and does nothing about a child, leaf or zone index outside its table — different failures, and on the two empty-zone-table maps § 4.2 counts, `roomForZone` is empty and step 6 indexed it. Every index is now bounded and INV-3 covers both. **Loop 1's narrowing guard was false**, found by two lanes: refusing a leaf's zone for membership does not make the `uint8_t` narrowing safe, since a file declaring 300 zones passes membership and aliases 300 onto 44. § 6 now refuses a zone table exceeding the engine ceiling as its own row, and a refused leaf stores `ZONE_REFUSED`. **Two lanes found the § 4.4 claim that the model's own box is "the only box the tables offer" simply false** — `Model::bounds` is per-node, indexed by `iCollisionBound`/`iRenderBound`. The sentence now says no per-LEAF extent, says why node boxes are not used, and § 14 carries the optimisation. **Two lanes found `roomsWithoutFootprint`'s own doc comment contradicting INV-6, § 4.4 and § 6** — it said rooms that had samples and failed to trace, where every other statement says rooms that caught none; an implementer reading the field would have dropped exactly the case INV-6 exists for. **Two found `bands[i]` undefined**: a cluster is a set of midpoints and "the lower edge" has two readings that place a room differently at every boundary, which two conforming builders would serialise differently — it is now the cluster's lowest midpoint, with the top band unbounded. **Two found an unsampled room's `minZ`/`maxZ` are struct defaults**, so it clustered at zero and invented a band below the level; such rooms are now excluded from clustering and join band 0. **One found § 6 claiming `buildRoomMap` returns the reader's error** when § 4.1 gives it a `Model` and never a package. **One found my own arithmetic in loop 1's option comment**: 32 was called a quarter of 64. **Collateral from these fixes, caught by my 4b sweep rather than by a lane:** § 10's INV-3 row still named one test, § 6's empty-table row still credited INV-4 for a bound that is now INV-3's, and § 4.4 promised a § 14 open question that did not exist. 663 to 729 lines, below the largest sibling in `docs/specs/`. **Size is not the diagnosis here; oscillation is.** |
