# UTA-0121 — bot path seeds for UT99's maps — loop log

The review record for `docs/specs/UTA-0121-bot-path-seeds.md`, kept outside
the spec per `~/.claude/standards/spec-format.md` § 6. `review-contract` writes
one row per loop as it closes. Rows are never back-filled and a landed row is
never edited.

## Cold-eyes loop log

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|------|------|-------|----|----|----|----|---------|
| 1 | 2026-09-11 | 3 | 1 | 5 | 0 | 1 | 7 verified, 0 dismissed, 7 fixed. A route's last spot yields to a navigation point within 50, which takes its place in the chain, and the next hop is measured from it. Routes are `none` only when nothing of the start's part is on the walk graph. A hop must also miss every mover box and keep walkable floor under it. Positions are rounded once to float and written as that float. The "Scout didn't fit" premise is deleted: their GAME-0023 is a crash with no seeds, not a refused seed; the Scout's test size is § 14's open question. Spots on one ramp join past the step. INV-4's wall is thin enough that both spots exist. Two sentences the fixes left wrong, § 3 decision 5 and § 8's editor claim, were corrected in the same pass. Also this loop, an authoring edit, not a finding: UT_MonsterHunt asked that a map with no file in `Maps/` be skipped, not refused, and confirmed § 14's four questions. Of the open questions, the Scout's size became § 14's; the material lookup, coplanar nodes and their seed grid resolved clean. |
| 2 | 2026-09-11 | 3 | 0 | 3 | 2 | 0 | 5 verified, 0 dismissed, 5 fixed. The run summary's entries are pinned (status written, skipped or refused; why; exits and nodes), and the summary is also written into the output directory, where UT_MonsterHunt reads. § 4.3's example numbers are written as `to_chars` prints them. On a `PARTITIONED` map the search stops at the part of the network that reaches the exit, so the chain bridges the gap without repeating that part; `propose` takes the group. A navigation point takes a spot's place only over an allowed hop. The directory summary came from lane 2's open question and lane 1's finding. The run-order and seed-grid questions resolved clean. At the cap, a spec's being 2, and a calm one: 2 of the 5 (the substitution hop and the directory summary) landed on text loop 1 wrote, the other 3 on the draft. The gated span is the whole document, so all 12 of the run's findings fall inside it. The tail is empty. Accepted at the cap. |
| 3 | 2026-09-11 | 3 | 0 | 0 | 0 | 1 | 1 verified, 2 dismissed, 1 fixed. A new run, gating UTA-0125's amendment (§ 3 decision 10, INV-11), loop 1 of its cap of 2. INV-11's test could not fail a scene that skipped the height test, or one whose mask lacked `R_SWIM`, `R_DOOR` or `R_PLAYERONLY`; it now uses seven specs, each on its own ordered pair of nodes, since `Scene::edges` holds pairs only: 1, 9, 32 and 85 kept; 2, a radius below 17 and a height below 39 refused. § 7's mutation list follows. Dismissed: all three lanes read decision 10's `Botpack.Bot` against decision 4's surveyor `Botpack.TMale1` as differing on swimming, but `Botpack/TournamentPlayer.uc`, which TMale1 inherits, turns on walking, jumping, swimming, doors and special moves, so both pawns give 125 and no line changes; the packet had omitted that file. And § 7's "only INV-9 and INV-10 need an install or a fixture map" omits INV-11, whose own clause names its harness, so nothing built changes. |
| 4 | 2026-09-11 | 3 | 0 | 2 | 0 | 0 | 2 verified, 2 dismissed, 2 fixed. Loop 2 of the UTA-0125 run, its cap, a spec's being 2. § 7 left INV-11 out of the tests needing a fixture map, so a builder following it could test INV-11 over an in-memory `Scene`, whose pairs cannot show the filter; it now names INV-11. § 13 said the real-asset case prints § 3 decision 4's numbers again, and neither § 7's print list nor the built test does; § 7 now asks for `Botpack.TMale1`'s resolved CollisionRadius, CollisionHeight and MaxStepHeight, and the test gains that print in UTA-0125's build. The same § 7 sentence was dismissed at loop 3 as changing nothing built; two lanes here showed it can. Dismissed: decision 10 cites `Bot.PreSetMovement` where the census surveyor runs `TournamentPlayer.PreSetMovement`, which both lanes raising it say gives the same 125; and § 4.3 and § 4.6's "descends from `MonsterEnd`" reads by name or by package, where the code and INV-10 already pin `MonsterHunt.MonsterEnd` and no census map was refused for lacking one. A calm cap: 0 of the final loop's 2 verified findings landed on text this run wrote. 1 of the run's 3 verified findings falls inside the change that armed the gate (commit 0fc8482); the other 2 are older. The spec is 580 lines; the tail is empty. Accepted again at the cap. |
| 5 | 2026-09-12 | 3 | 1 | 1 | 0 | 1 | 3 verified, 0 dismissed, 3 fixed. A new run, gating UTA-0127's amendment (the per-exit `offWorld` mark: § 4.3, § 4.6, § 5's INV-12, § 6, § 7, § 9, § 10, § 11), loop 1 of its cap of 2. All three lanes independently found the same Q2, and it was in text this amendment had just written: § 4.3 and § 6 asserted that an off-world exit's route IS `none`, while INV-12 said it keeps whatever word `propose` gave it. § 4.7 does not produce `none` by rule -- on a `PARTITIONED` map the fallback goal is the part reaching the navigation point nearest the exit, and `nearestNode` applies no distance cap, so an exit at the world corner still acquires goal spots and can return `found` with a node chain. An implementer would have written `if (offWorld) route = none` before serialising, discarding a route the search did find and flipping `moverOnly`. Fixed toward the search rather than against it, which is also what the mark was chosen for: § 4.3 and § 6 now say the exit is searched like any other and keeps § 4.7's word, and § 6 names why a single-exit off-world map is `none` anyway -- § 1 defines `EXIT_OFF_NET` as the exit being far from every navigation point, so such a map is never `PARTITIONED`. The case needs a second exit near the network; UT_MonsterHunt measured that all 58 off-world maps carry exactly one MonsterEnd, so it is reachable by construction and not live today. INV-12's test ran through `toJson` alone while asserting what `propose` returned, so the assertion could not fail [Q4]; it now runs `propose` then `toJson` over a partitioned scene of three exits, which is the case the lanes found. § 11 claimed UT_MonsterHunt's census reads `offWorld` [Q1]; agreed with them the same day, but their parser does not read it yet, and `schema` staying 1 is what lets it keep working until it does -- now stated in the future tense with that reason. Open questions: how the census groups a multi-exit map resolved clean against § 1's own definitions; whether `nearestNode` caps distance resolved against the code and confirmed the finding rather than clearing it. Also this loop, an authoring edit and not a finding: § 4.6 asserted UT's world extent as fact with nothing in this repository backing it, so it now says the threshold rests on the measurement and that no UT source here states the bound. |
