# `docs/design.md` — review record

Loop log for the `review-contract` gate on the project design document.
Kept here rather than in `docs/design.md` itself: that document has never
carried one, nothing requires it to, and a review table is not what its
readers came for.

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|---|---|---|---|---|---|---|---|
| 1 | 2026-09-03 | 3, cold — genre pinned `adr`; packet carried discovery, the ADRs, the versioning override, windows on `workflow.md` §§ 2–4 and `versioning.md` §§ 3–4, and the machine's measured toolchain | 2 | 3 | 5 | n/a | **Ten verified, ten fixed; none dismissed.** First gate on this document, and on the project. **All three lanes independently found the same two defects.** `ued` sat under `### Runtime` while rule 10 granted it "anything" and rule 2 forbade every runtime target the three build-time parts — so an implementer either linked `upkg` into `ut-ed` or routed every package read through `ut-bake`, two different link graphs. And `M6` resolved to nothing: the `M<n>` scheme existed only in conversation, while `versioning-overrides.md` labels milestones by version — so a roadmap item written against it bound to no release, and read as `0.6.0` it deferred the weapon-wheel decision past the release **S9** makes the wheel a cut criterion for. **Rule 3's "only" was false twice over**, found separately by two lanes: `unav` and `ubundle` both claimed the graphs with no rule ordering them, and `urecipe` sat in the runtime group while being a bake input. **The sharpest single finding was that no rule made ADR-0003 true in code** — rule 2's link ban stops the runtime *reading* a `.unr` and does nothing about *writing* a bake into the repository, which is the failure that ADR calls permanent in a public history; the sentence crediting "the first three" rules with it also named rule 1, which relates to neither ADR. Two more gaps had no owner at all: what triggers a bake when a player joins, and what "a valid Unreal Tournament install" means to a runtime forbidden from linking the package reader. Both are now answered by shelling out to `ut-bake`. **One lane caught a content-addressing hole no other did**: `umat`'s curated material library was a bake input outside the bundle's hash, so a corrected library would let two players compute one name for two different worlds — fixed by shipping the library with the baker. **One finding was the orchestrator's, found while sweeping**: rule 16 put a bake on the join path, ADR-0002 makes a bake expensive on purpose, and **S5** promises a minute — so baking now happens ahead of need, with the cold case stated rather than hidden. **Collateral:** `versioning-overrides.md` listed "the editor's project format" as a breaking surface, an artefact the design does not have; replaced with the character package format. **Two stale counts were introduced by this loop's own fixes and removed before the commit** — a rule count and a group count, both censuses that go stale on the next edit. |
| 2 | 2026-09-03 | 3, cold — identical brief, packet rebuilt from disk | 0 | 5 | 5 | n/a | **Ten verified, ten fixed. Not one Q1, and every finding landed on text loop 1 ADDED** — the documented pattern at its starkest, and the reason this loop fixed by simplifying rather than by adding rules. **All three lanes found the same one, and it was the worst defect of the run**: loop 1 headed the new tools group *"never shipped to a player"*, and `ut-bake` is a tool the client must run — built as written, the packaged game would fail at `exec` on first contact with any map, taking **S5** and **S7** with it. **Two more were loop 1's rule 15**, which swept up the per-user config, the logs, and the character packages `0.6.0` exists to distribute, putting them in a quarantine that forbids serving them. **Loop 1's rule 16 invented an install manifest** and with it a fourth cross-seam format nobody owned and a first-run bootstrap nobody had specified; the fix deletes the manifest and has `ut-ants` run `ut-bake --check`, which already links the reader. **Loop 1's rule 3 forgot the editor writes**, so `ued` could read bundles and never save one. Two gaps predated loop 1: no rule said whether the dependency section was a whitelist or a list of prohibitions, so two builders would write opposite link checks; and the character package was a cross-seam format with no owner — now one `.utab` container for a baked map and an authored character alike, which also gives rule 15's guard an extension to test. One lane read ADR-0004 against `urecipe` and found the recipe could not carry the class override that ADR requires. **Collateral in a neighbouring document, and a decision it forced:** ADR-0003's *"served to another player"* clause forbade exactly the Monster Hunt rotation this project is for, since a host-download cache lives under `content/`. That clause carried two rules with different subjects — what the repository publishes, and what a running server sends — so **ADR-0006 splits them and supersedes it**, written as a new ADR rather than an edit, per `docs/decisions/README.md`. **One authoring change arrived mid-loop** and is in the text loop 3 reads: first-person platforming (**S11**), with visual assists and physical assists separated so the physical ones cannot cost **S2**. |
| 3 | 2026-09-03 | 3, cold — identical brief, packet rebuilt from disk and given ADR-0006 | 1 | 8 | 3 | n/a | **Twelve verified, twelve fixed. Cap reached (3 for an ADR); the run files its tail and exits.** **All three lanes found the same defect**, and it was loop 2's own fix contradicting itself inside one rule: rule 15 requires the maps and characters `ued` writes to live outside `content/` and be distributable, and its guard five lines later failed any `.utab` outside `content/` — which is those files. The fix is a predicate on origin rather than extension, and § Content addressing already supplied the discriminator, so nothing new had to be invented. **All three also found that rule 16 said *the runtime* and named one program**: a dedicated server must bake its own rotation, so **S8**'s host needs `ut-bake` and an install exactly as a player does, and packaged as written it would have had neither. **The sharpest single finding was that loop 2's platforming text failed its own sign.** **S11** asks a UT99 player to check **S2** *on the same server*, and the physical assists were defaulted on in Monster Hunt — so the second half failed by construction. They now default off everywhere and **S11** is measured at the defaults, which is closer to the Metroid Prime original anyway: that game's answer is mostly showing, not changing. **Two lanes found the camera-pitch assist called simulation-free while being a view-angle change** — view angles are aim, and aim is server-authoritative; it is now a render-camera offset with the crosshair on the untouched aim ray. **One lane found rule 4's *checked the same way rule 2 is*** to be false: rule 2's check is a link closure over both runtime targets, and applied to rule 4 it asserts the client links no renderer, which rule 5 requires it to. It is a forbidden module edge. **Three findings were absences the stack table hid** — audio and the in-game UI had no row at all, so the GPL-3.0 obligation and the CMake dependency set rested on a guess; server discovery, which the whole pre-bake plan leans on, had no owner; and ADR-0004's global class-override list was a bake input outside the hash. **Collateral, and two of it the orchestrator's own check rather than a lane's:** the design was narrowing **S5**, a sign `docs/discovery.md` owns, so the qualifier moved to the sign; `versioning-overrides.md` cut **S9** at `0.2.0` when **S9** asks for a Monster Hunt round that does not exist until `0.4.0`; and **S7**, **S10** and **S11** were claimed by no release at all until a coverage sweep of the table caught them. **Calm or violent:** eight of twelve landed on text this run wrote, which is the oscillating shape — but four of those eight are the platforming requirement, which arrived mid-run as authoring rather than as repair, and the loop's yield fell from ten to twelve findings across a document that grew by half. **Read as calm**, with the tail empty: every finding was fixed, and the document is routed to implementation rather than to a fourth loop. |

**A new run opened after the level map (S12) was added as an authoring
change.** The previous run closed calm at its cap, so re-arming it is
ordinary rather than wasteful — `CLAUDE.md` rule 14.

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|---|---|---|---|---|---|---|---|
| 4 | 2026-09-03 | 3, cold — genre pinned `adr`; packet rebuilt from disk with ADR-0006 added. First loop of a new run | 1 | 4 | 3 | n/a | **Eight verified, eight fixed.** **All three lanes found the run's most consequential defect, and it would have broken the project's own purpose**: ADR-0006 tells Epic's content from the community's by *what the baker read out of the player's install*, and the 610 Monster Hunt maps live inside that install — so the test classifies every one of them as Epic's and forbids serving the rotation this project exists for. The lanes reached it from a packet fact about the directory layout rather than from the prose, which is what the *Verified source facts* section is for. It also had no owner and no field: rule 15 now has `ubake` record a `stock` / `community` / `authored` origin per bundle, decided against the finite list of packages Epic shipped, read by both the guard and `unet` — one field closing the classification hole and the guard's discriminator, which two lanes filed separately. **All three also found a contradiction created by the previous run's own last loop**: loop 3 moved **S9** to `0.4.0` in `versioning-overrides.md` and left the design justifying the weapon wheel's deadline by **S9** cutting `0.2.0`. **One lane found a Q1 in the platforming text that is a fact about geometry**: with the render camera pitched down and the aim ray untouched, the aim direction sits *above* the camera's forward axis, so the crosshair rises rather than falls — and that sentence is the stated proof the assist touches the simulation nowhere, so it was load-bearing. **The level map's own arrival left two loose ends**, found by two lanes: `umap` joined the seam group while rule 3 still said three vocabularies *and there are no others*, and rule 17 gave a direction to `unav` and not to `umap`. **One lane found a deadline that could not be met**: deferred decisions were to be settled *before* the release naming them, while two of them wait precisely because the thing to judge does not exist until that release is under way; they are now settled at its start, against a prototype. **Collateral:** ADR-0006's Status now points at the correction, its decision left standing and its reasoning unedited, as `docs/decisions/README.md` requires; and `versioning-overrides.md` stopped calling the bundle name a *hash triple*, which an authored bundle's two-input name had already made false. |
| 5 | 2026-09-03 | 3, cold — identical brief, packet rebuilt from disk | 1 | 6 | 2 | n/a | **Nine verified, nine fixed, and the largest fix DELETED a concept rather than adding one.** **All three lanes found loop 4's guard**: it failed a `.utab` whose origin was `stock`, and a `community` bundle is still baked from the install, so a bundle derived from somebody else's map passed the guard and `.gitignore` alike and could land in a public history permanently. **One lane found why the three-value origin could not work at all**, and it is the finding of the run: a bundle has many sources, and a community Monster Hunt map draws on Epic's stock texture packages — so `ubake` would stamp it `community` while its materials were Epic's throughout, and the server would ship exactly what the quarantine exists to stop. Widening it to *stock if any input is Epic's* fails the other way, since that is every community map. **The fix removes the third value and stops bundles travelling**: origin is `authored` or `derived`, inherited from the most restrictive input, and `unet` sends a map as the community's own package plus our recipe for the joining player to bake — which is ADR-0003 § Consequences, a clause ADR-0006 never superseded and loop 4 had drifted off. Two lanes had filed that drift separately. It also settles, without another rule, what a `ued` edit of a baked map is. **One lane found the headless server would link Vulkan**: rule 4 forbids the edge out of `uworld` and nothing forbade `ugame` reaching `uui`, so both stated checks passed while `ut-ants-server` pulled in a graphics driver it must not need. Rule 2's closure test now names those three parts for the server. **Two lanes found step-up on both sides of one line** — ADR-0001 names step-up height among the constants **S2** requires, and loop 2 had listed it as a physical assist defaulting off, which at the defaults **S11** is measured at would stop a player on every staircase. **One lane found the global class-override list called a bake input**, which would invalidate every cached bake on a large rotation to fix one monster; resolution happens at spawn, so it is game data and touches no bundle. **A lane's open question was right and cost three claims:** the install holds 612 maps of which 515 are Monster Hunt, not 610 Monster Hunt maps. Corrected in the live documents; **the three ADRs and the landed loop-log rows keep the figure they were written with**, per `docs/decisions/README.md` and this log's own rule. Five roadmap items were amended in the store to match. |
| 6 | 2026-09-03 | 3, cold — identical brief, packet rebuilt from disk, install facts corrected to 612 maps / 515 Monster Hunt | 0 | 5 | 6 | n/a | **Eleven verified, eleven fixed. Cap reached (3 for an ADR, this run's third); the run files its tail and exits. Not one Q1.** **All three lanes found the gap loop 5's own simplification opened**: collapsing origin to two values removed the test a *server* needs. Origin describes a bundle, and what travels for a map is a package — so an implementer building `unet`'s send path had nothing to code against, and shipping Epic's stock maps to joining players satisfied every sentence in the document. Two questions now take two answers: origin governs the repository guard, and a shipped **stock manifest** governs the wire, refusing to send a package it cannot identify. **The second cluster was `ued`'s output**, which one lane showed had three incompatible readings and another showed contradicting rule 15's own placement sentence; settled the way ADR-0003 already implied — editing somebody else's map produces a *recipe*, editing geometry produces an `authored` bundle, and a `derived` bundle is never published. **One lane found the replication timing stated three times and two ways** — join in two passages, map change in the third, the one the others cite — so **S10** would have failed for every player already connected when the rotation advanced. **One found the guard would have blocked every push on this machine**: *an Unreal asset anywhere* scans a working tree that holds the player's own install under `content/ut99/`, and the extension list was written twice with different contents. It is now three checks over tracked paths, with one list read by both `.gitignore` and the guard. **One found aim assist called a tick input when it is not** — it transforms view angles the command already carries, before the command is sent — which is what lets it wait for `0.3.0` while the weapon wheel cannot. **Calm or violent:** eight of eleven landed on text this run wrote, which reads as oscillation until the shape is looked at — each loop's findings moved from structure to mechanism to detail, and this loop's are the smallest and most local of the six. **Read as calm.** The tail is empty; every finding was fixed. **Routing: the document goes to implementation, which is the better third reviewer, and not to a fourth loop.** Three roadmap items and one breaking-surfaces list were amended as collateral. |

**Run closed at the cap.** Across six loops: eighteen cold lanes, fifty-one
verified findings, fifty-one fixed, none deferred. Four times all three lanes
of a loop independently found the same defect. The document is routed to
implementation; a later authoring change re-arms the gate normally.

**A new run opened when Windows became a first-class target.** The previous
run closed calm at its cap and routed the document to implementation; this is
an authoring change of direction, so the gate re-arms normally — `CLAUDE.md`
rule 14. Capped at one loop at the user's direction.

| Loop | Date | Lanes | Q1 | Q2 | Q3 | Q4 | Outcome |
|---|---|---|---|---|---|---|---|
| 7 | 2026-09-04 | 3, cold — genre pinned `adr`; packet carried discovery, the six ADRs, the versioning override, and the verbatim build files (`CMakeLists.txt`, `tests/CMakeLists.txt`, `scripts/ci.sh`, `scripts/quarantine-guard.sh`, `ci.yml`) plus the tracked-file list. The unrunnable region — MSVC, Windows paths, the Visual Studio generator — was stated up front, so Q1 was out of scope there | 0 | 2 | 5 | n/a | **Seven verified, seven fixed; none dismissed, one surfaced.** Not one Q1: every defect was two passages disagreeing or a rule nobody could implement one way. **All three lanes independently found the same Q3**, the strongest signal this gate produces: `unet` "sends only a package the manifest does not list, and refuses to send one it cannot identify" never said whether the key is the name or the hash, nor what *identify* means — one reading puts a patched Epic package on the wire, another withholds every community map and fails **S3** and **S5**. It now withholds by name whatever the hash, withholds what it cannot read, and sends the rest. **Two lanes found the `ued` origin contradiction**: editing a baked map's geometry was `derived` under rule 15 and `authored` twenty lines later, and origin decides whether a bundle travels whole — so one reading publishes a re-lit Epic map, the exact breach ADR-0003 exists to prevent. Origin is now inherited from the input bundle, never from what was edited. **Two lanes found Clang 18 in the compiler floor with no job building it**, which the new two-platform gate made visible; the matrix gains a Clang leg and the paragraph now says what the gate builds rather than counting compilers. **The sharpest single finding came from one lane and is a direct consequence of the change being gated**: rule 9's "the same flags" is unachievable across MSVC and GCC, and the determinism bullet's three mechanisms — global state, frame timing, container order — cover no floating-point behaviour, while ADR-0002 requires one map, recipe and baker version to hash to one bundle *on any machine*. Prediction and bundle naming both now rest on a stated numeric contract. Two more, one lane each: the jump-arc landing marker lived in `uui`/`urender`, which own no simulation, so an implementer writes a second integrator that drifts the first time a `uworld` constant is tuned; and `urecipe`'s class overrides appeared in neither the hashed nor the runtime-read enumeration, so hashing them would invalidate every cached bake to fix one monster. **Two findings were the orchestrator's own**, found while verifying rather than by a lane: the new platform sentence licensed a platform API "in any part", contradicting rule 1's `core` depends on nothing beyond the standard library; and it counted compilers, which goes stale. **Collateral, caught by the 4b sweep and by nothing else**: `versioning-overrides.md` carried a verbatim copy of the manifest rule and was left stating the old one — consolidated to a pointer rather than a second copy. **Surfaced, not fixed:** the stack table settles dependency acquisition for Catch2 and Dear ImGui and for nothing else, which a Windows target makes load-bearing against **S7**; picking FetchContent, vcpkg or `find_package` is a decision, filed as `UTA-0043`. **Out of scope, reported not carried:** three ADRs give the map count as 610 where `discovery.md` says 515 of 612; the design cites no count, so it is the ADRs' to settle. Empirical claims the fixes added were executed rather than asserted — the whole gate ran green under Clang locally, so the new leg is not shipping red, though local Clang is 22 and the job pins 18. **Packet gaps, the orchestrator's own:** `.gitignore` and `LICENSE` were not windowed, and two lanes correctly raised both as open questions; neither changed a finding — `LICENSE` is GPL-3.0, and no quarantine glob matches `.utab`, so an authored bundle outside `content/` passes the guard. Four other open questions resolved clean and are not in the tally. |
